# Trials of Kirtas — Context & Handoff

A working context doc for **M** (developer / DM / musician) and **C** (Claude, technical collaborator). Hand this to a fresh session to continue without re-explaining.

---

## Who / what

**Project:** *Trials of Kirtas* — a D&D 5e virtual tabletop. Stack: vanilla JS/HTML/CSS, Supabase (Postgres + Realtime), Netlify (incl. serverless functions), GitHub. M commits and deploys **manually**; C reads the codebase, proposes plans for approval, and writes incremental changes.

**M (personal):** sarod player in the Hindustani classical tradition (Maihar gharana), involved in preservation/transmission and archival work. Keeps a deep Obsidian vault ("The Codex"). Cares about thoughtful tool design. *Do not make confident assertions about Indian classical music terminology without appropriate uncertainty.*

---

## How C works on this project (working rules — carry forward)

- **Read the actual source from the repo before editing** anything. Clone/pull `main` first.
- **`node --check` before presenting** any JS/HTML (for HTML, extract the inline `<script>` and check it).
- **Never commit or push.** C hands files over; M commits and deploys manually.
- **Ask and get explicit approval before writing feature code.** Lock the design first.
- **Show a mockup before building any layout decision.**
- **Never change a theme var to fix a per-page problem** — hardcode page-specific overrides on the page itself. Reuse existing theme vars for *new* UI.
- **Any Supabase table created via raw SQL needs an explicit `GRANT`.** House style is `grant ... to authenticated` (an older note said `anon, authenticated` — the live tables only grant `authenticated`; match the live tables).
- **Realtime checklist:** table in the `supabase_realtime` publication + socket authenticated via `setAuth` (re-applied on token rotation) + `replica identity full`.
- **Provision Supabase `profiles` by auth `uid`, never by assuming email.** A seed keyed on `email` silently no-ops if run before that user's first login (the `auth.users` row doesn't exist yet for OTP). `role` (privileges) and `character_key` (which PC) are **independent** — staff/Narrator correctly has `character_key = null`. There is no `narrator` role; `dm` *displays* as "Narrator". (See the Auth/profiles playbook below.) **Backfilling/repairing by email-join to `auth.users` ordered by `last_sign_in_at desc` is duplicate-safe** (picks the active login) and needs no access to the user's machine.
- **A write policy granted `to authenticated` gates on "logged in," not "is a member."** With open OTP that's anyone → gate party/character WRITES on `public.is_member()` (overseer/dm/player), not bare `authenticated`.
- **A SECURITY DEFINER RPC invoked with the SERVICE key has no `auth.uid()`** (service role isn't a user), so `is_overseer()`-style self-gates inside it fail. Call overseer-gated RPCs from the BROWSER with the user's session token; reserve the service key for reads the client can't do (e.g. listing `auth.users`).
- **"nothing to commit" is a red flag** that a file never actually changed, not a success.
- Don't reverse a correct earlier refusal/decision under pressure; judge cumulative output.

---

## Architecture (locked)

- **Three-role model:** `overseer` / `dm` / `player`, enforced via Supabase RLS + a column-guard trigger (`combatants_guard_columns`) and `public.is_staff()`.
- **`combatants` table is truth during combat**, with `source_key` binding to character data. Column guard pins staff-only fields from players (owner, name, side, hidden, max_hp, **disposition**, **in_combat**); players may change hp, conditions, x, y, **initiative**.
- **`combat.html`** is the dedicated live battle-map page.
- **`battle.js`** is the cross-page HUD; it's backend-agnostic and shares state through a **pluggable backend seam** (`window.__battle.useBackend({...})`). `combat.html` hands it a Supabase `combatants`-backed backend (`load`, `save`, `saveConditions`, `setInitiative`, `advanceTurn`, `logRoll`, `subscribe`). The turn/round bridge (`setTurn`/`advanceTurn`/`pushTurnToHud`/`activeSourceKey`) is on `main`. **Option A (done): `combat.html` AND `party.html` now install ONE shared backend — `combatants-backend.js` (`window.makeCombatantsBackend(deps)`) — via `useBackend`; write logic lives once, each page injects its own glue (cache/channel/logger).** The turn engine is the **`advance_turn()` `SECURITY DEFINER` function** both surfaces call via `rpc('advance_turn',{p_encounter})` — computes next from the FULL roster (RLS-safe), server-side off-turn guard, logs the single masked replay turn event.
- **One active encounter at a time** (`encounters`, uuid PK).
- **`public.characters` table is the live source for character-sheet data** (`structural` / `vitals` / `inventory` / `equipment` / `currency` / `bio` / `notes`), same RLS + column-guard pattern as `combatants` (`characters_guard_columns` pins identity cols `key`/`owner`/`created_at`; everything else owner-editable). Git `data/characters/<key>.json` stays the export backstop. **`party.html`** is the immersive character-sheet takeover (portrait wall → in-page game menu), reading live via **`character-data.js`** (`CharacterData.loadParty` / `loadCharacter` / `canEdit`, on `window.__tok.sb`).
- **Combat↔sheet↔orb realtime sync (June 22 — Option A's combat half).** `combat.html` now runs a separate **`combat-characters`** realtime channel (mirrors `party.html`'s `party-display`): on a `characters` UPDATE it re-blooms the open token-orb ring (**`CombatOrbs.applyRow(key,row)`** — updates cache, re-blooms, deep-equal self-echo guard so the orb's own optimistic write doesn't flicker) and re-renders the open floating-sheet tab **in place** (**`CombatSheets.refresh(key)`** → `__sheet.renderSheet`, which only repaints values + handler-free display lists). The **floating sheet** is **`combat-sheet-float.js`** (`window.CombatSheets.open(key,name)`/`close`/`closeTab`/`refresh`/`onAdd`) — a draggable/resizable **multi-tab** window mounting a LIVE v11 sheet per tab via `window.mountSheet(page,key)`; geometry persists to `localStorage tok.sheetFloat.v1`. **Open caveat:** **conditions and initiative are `combatants` fields, NOT `vitals`**, so they do NOT ride this channel — the **combatants-realtime handler must re-bloom the orbs** for those (next session). The ribbon **"+"** needs a party-picker (fires `onAdd`/`combatsheets:add`, nothing listening — inert).
- **Right dock on `combat.html` (UX-AUDIT-1/1b):** one collapsible panel with **icon-over-label tabs** (Feed · Display · Bestiary · Combat · Scenes; staff tabs absent for players) — `buildDock`/`dockRegister`/`dockOpen`, `dockRegister` takes an optional short `tab` label ('Game feed' → 'Feed'). Eye (view-as-player) + minimize ✕ live in the tab-row corner. Collapsed = a slim ❮ right-edge handle with an unread dot; the handle reopens to the last tab. A **roll ticker** (latest roll, plain-text-stripped body, click → feed) shows whenever the Feed pane isn't visible; it seeds from the newest archived roll and inherits hidden-roll masking (RLS + an `effectiveStaff()` check for the eye preview). Tab row is overflow-proof (`flex:1 1 0; min-width:0` + label ellipsis; corner `flex:none; margin-left:auto`) — a long pane title once pushed ✕ off-screen; don't regress this. Statblock slide-out now anchors `right:0` (the rail is gone) and KEEPS its visibility gate (its closed `translateX(100%)` state parks exactly where the handle + ticker live and must not eat clicks — that bug shipped once). Zoom bar at `right:12px`. Clicking the active tab does NOT collapse (deliberate change from the rail's toggle; ✕ is the close).
- **Roll-modifier seam (battle.js, public):** `RS` state (adv/dis/bless/guidance) drives all HUD rolls; `window.__battle.toggleRS(key)` / `getRS()` / `onRSChange(cb)` let a page host its own toggle surface. On `combat.html` the feed's mods row IS that surface and the roller popup is hidden (page-specific CSS); every other page keeps the roller.
- **Cross-page roll logging:** `feed-bridge.js` (loaded after `battle.js` on all 8 non-combat pages) sets `window.__battle.onLogRoll`, the fallback battle.js uses when the backend lacks `logRoll`. It posts HUD rolls to the feed with session + active-encounter stamps (30s-TTL context cache). `combat.html` never loads it; its full backend wins regardless.

---

## Key facts

- **Party characters** (source_key): Cosmere Runestar (`cosmere`), Caim, Líadan Luchóg (`liadan`), Vesperian Vale (`vesperian`). *Tyros → Cosmere rename is FULLY complete (verified in-repo): `data/characters/cosmere.json` exists, the `KEY_FILE` alias was removed 2026-06-01. Remaining "tyros" mentions are deliberate or inert — chronicle.html's legacy portrait mapping (old entries authored as Tyros resolve to the cosmere portrait — keep), historical archive data in `chronicle.json`, and the dead `sheet-prototype.html` — **already absent from the repo** (confirmed in the June-25 audit; the "delete-someday" note is moot).*
- **User → role/character:** `thebraveruby@gmail.com` → overseer + Vesperian; `hagakuredisc@gmail.com` → DM, no character; `ianakira@gmail.com` → Cosmere; `jayvanmidde@gmail.com` → Caim; `nazanroseaktas@gmail.com` → Líadan.
- **Initiative modifiers** live in `characters.js` as `CHARACTERS[key].combat.initiative` (e.g. Vesperian +4). NPC init mod = `Math.floor((statblock.dex − 10) / 2)`.
- **Chronicle** (`chronicle.html`) is the **server browser** (Phase 3A): a Discord-style channel tree (sessions → #chronicle / ⚔ per-encounter / 🎲 table-talk, plus staff-pinned TAG channels), the original long-form reading view as the default "All Entries" landing, paginated combat channels with round dividers + encounter banner, server-side cross-channel search, and staff drag-ordered channels — all over the unified Supabase feed. Quill entries/authors/tags/@mentions/images unchanged. The git-committed `chronicle.json` remains the durable export backstop (nightly + button).
- **Signature resources (combat orbs + sheet):** pool *maxes* derive from `structural` via `resource-derive.js` (`window.ResourceDerive.derive`); **only SPENT is stored**, in `vitals.pipState` (keys `ki`/`bardicInspiration`/`superiorityDice`/`starlightStep`); current = max − spent. The **sheet displays** pools (read-only); the **orb spends** them. Player extras live in `structural.customResources` (`[{id,label,max,recharge}]`, `max` a formula token → auto-scales on level-up). Spell slots / sorcery points are **not** derived yet (static in Spellcasting).
- **LESSON — orb/element class names must not collide with global classes.** A combat orb's kind class `sheet` collided with `sheet-mount.css`'s `.sheet` (the character-sheet *panel*: `padding:30px 32px 22px` + `hue-rotate`), silently inflating the orb to 67px and surviving every width fix — the orb's *width var was honoured the whole time*; the padding was the inflator. Before adding a bare class to a shared/floating element, grep the loaded stylesheets for a rule of that name. Orbs are now `torb primary` / `torb hp` / … (never a bare `sheet`).

---

## Live DB / env state

- `schema_delta_feed.sql`, `schema_delta_chronicle_unify.sql`, and `migrate_chronicle.sql` are **run and verified live** (feed streams, migrated chronicle entries with images render in the combat feed).
- `SUPABASE_SERVICE_ROLE_KEY` should be in Netlify env — **verify the Export archive button has been pressed once successfully** (round-trips `chronicle.json` through the feed-export function).
- **Open question (still):** confirm the `disposition` column exists on the live DB before building the disposition feature code.
- **`schema_delta_characters.sql` + `migrate_characters_data.sql` + `grant_service_role.sql` are run & verified live.** `public.characters` is the live source for character-sheet data; the export backstop round-trips it to `data/characters/<key>.json` (manual trigger = `200 {committed:4}`).
- **TWO LESSONS pinned (from the export saga):** (1) raw-SQL-created tables must be granted to **`anon, authenticated, service_role`** — the missing `service_role` grant on `profiles`/`characters` caused the entire `42501 permission denied` saga (the dashboard auto-grants all three; raw `CREATE TABLE` does not). (2) Supabase's new `sb_secret_`/`sb_publishable_` keys are **NOT JWTs** → put them in the `apikey` header only, never `Authorization: Bearer` (which expects a JWT and silently drops to anon); legacy `eyJ…` JWT keys go in both. `svcHeaders()` in the export core handles the split. **Latent:** the `feed-export` staff button shares the gap — the `profiles` grant unblocks its staff check, but `feed`/`campaign` would still need `service_role` grants if that button is used.

## This session (June 22) — sheet **rest/hit-dice recovery**, combat-board fixes, the **floating sheet** (`CombatSheets`) finally built, and **combat↔sheet↔orb realtime sync** (the half `combat.html` was missing)

**Sheet rest recovery + hit-dice (DONE, deployed, verified).** The v11 sheet's short/long-rest cluster (`data-rest="short|long"` + `#insp-toggle`, in `sheet-mount.js`) is now live with a confirm-popover + **pre-rest undo snapshot** (`#insp-stat`). `resource-derive.js` gained `deriveHitDice(structural)` (pools largest-die-first + total, on `window.ResourceDerive`); `sheet-mount.js` renders a live **HD medallion** (`#hd-med`, `renderHitDice`); `sheet-actions.js` was rewritten with pure planners (`planRest`/`rollHitDie`/`shortRestKeys`/`longRegainCount`) + `wireInspiration` extended to wire rests (confirm + undo) and HD-spend (popover on `#hd-med`). **5etools-2014 verified:** HD faces per class; short rest = spend HD (roll + CON, min 0); long rest = full HP + regain ⌊total/2⌋ min 1 largest-first; short rest zeroes short-recharge pipState keys. HD derive **parses `structural.combat.hitDice`** (the git export's `structural.classes[]` is absent) with a verified CLASS_HD map for the `classes[]` path. Smokes: `smoke-sheet-resources.mjs` 20/20, `smoke-sheet-actions.mjs` 51/51.

**Combat-board fixes (DONE, deployed, verified).** All in `combat.html` (+ `combat-orbs.js`, `combat-sheet-float.css`): **(1) board blur** — removed the static `will-change:transform` that cached a 1× texture `scale()` then upscaled; `applyTransform` now toggles `will-change` on *during* a gesture and drops it to `auto` ~220ms after settle → crisp at rest. **(2) token-name placement** (Option A — name & orbs as separate modes): `@media(hover:hover){ .token:hover:not(.selected) .token-name{display:block} }`; selecting (orbs open) hides the name; touch shows nothing (long-press context menu names it). **(3) orb-config menu scroll + drag-reorder** — root cause was the orb config trapped in a cramped nested scroll (`.ctx-exp-body{max-height:230px}`); removed that cap so the menu's single 70vh scroll handles it, + a `wheel`→`stopPropagation` on the menu so wheeling never bubbles out to zoom the board; the ▲▼ buttons became a `.torb-grip` drag handle with **document-level** pointermove/up (binding to the grip dropped tracking once the cursor left it).

**The floating sheet — BUILT (it was the gap).** `combat-sheet-float.js` / `window.CombatSheets` was *referenced* by `combat.html` and its CSS was fully designed, but the **JS was never written** — so the sheet orb, the "Open sheet" menu item, and the party-row link all silently no-op'd (`CombatSheets?.open?.()` → undefined). Now: a draggable/resizable **multi-tab** floating window mounting a **live v11 sheet per tab** via `window.mountSheet(page,key)` (same call `sheet-v2.html` uses), so each tab reads the live `characters` row through `CharacterData` and carries its own write-affordances. Tabs (switch/close), drag (`.sf-grip-l`, document-level), resize (`.sf-grip`), minimize, close-to-edge-tab; geometry → `localStorage tok.sheetFloat.v1`. API: `open/close/closeTab/refresh/onAdd`.

**Combat↔sheet↔orb realtime sync — BUILT (Option A's missing combat half).** The decision was *already locked* (Option A, June 15–16: characters = single truth, ONE `combatants-backend.js`, the bidirectional HP-mirror trigger) — **don't re-litigate it** (C started to this session; M correctly stopped it — the spine is in this doc). What was actually missing: **`combat.html` never subscribed to the `characters` table** (party.html has the twin `party-display` sub). The orbs + floating sheet are *newer* than the Option-A work, so combat had no read-path to refresh them on a vitals change. Added a separate **`combat-characters`** channel → on a `characters` UPDATE it calls `CombatOrbs.applyRow(key,row)` (re-bloom + self-echo guard) + `CombatSheets.refresh(key)` (in-place `renderSheet`). Writes were already correct everywhere (orbs + sheet + HUD backend all write `characters.vitals`); this is purely the read/refresh wire. `node --check` clean (both modules + the combat.html inline block). **M: "mostly working."**

**OPEN ISSUES (→ next session, in priority):**
1. **Orb sync is partial — combatant-field changes don't re-bloom the ring.** Slots/vitals sync (they ride the new `characters` channel → `applyRow`). But **conditions** and **initiative** live on the **`combatants`** row, *not* `vitals`, so they DON'T go through that channel — they only refresh when you press the orb (which re-blooms). HP appears to sync (likely via the existing token-HP path). **Fix:** the existing **combatants-realtime UPDATE handler should also re-bloom the open orb ring** for hp/conditions/initiative (it already updates the token; extend it to call a `CombatOrbs` refresh). That unifies the sync.
2. **The PCs' `characters.structural` in the DB is WRONG — an aggregate, not their real builds.** `sheet-v2` shows the *same* data across PCs (everyone has Hexblade curse etc.) → it's not per-character, and the **orbs read incompletely as a result** (AC missing, wrong resources). The four PCs' real builds were never properly populated; the rows carry bad/placeholder `structural`. **This is the root cause of both the wrong sheet AND the thin orbs.** **Fix:** rebuild the four PCs' real data — **M leans: re-make them from Roll20** directly into correct `characters.structural`. Connects to #4.
3. **The right rail is combat-only and the OLD version.** The agreed design (see **THE RIGHT RAIL** below — `mock-right-rail-v1.html`, *loved*: docked slide-out, tabbed **Feed/Sheet/Codex/Settings**, mounted **site-wide** by a shared script like the HUD overlay, v11/Metaphor themed) is **not** implemented site-wide — only a stale combat version exists. **Fix:** build the shared site-wide rail in the new design.
4. **The Soul Shards Forge can't save characters.** `shards.html` derives `structural` but is still **preview-only** — the **gated write** (`CharacterData.save(key,{structural})` + `character_snapshots`, the back half of P7) was never wired, so **new characters cannot be saved**. **Fix:** wire the Forge write — OR (M's call) skip ahead and just **rebuild the 4 Roll20 PCs** into the table directly to unblock #2, deferring the full charactermancer.

## This session (June 21) — **signature resources** (derived + player-authored), **token-orb sizing + size sliders**, and the **`.sheet` class-collision** that masqueraded as a sizing bug

**Derived signature resources — SHIPPED & DEPLOYED.** Resource pool *maxes* now derive from class/level/abilities/race per 5e rules (not hand-seeded), so level-ups and stat bumps flow automatically. Verified the stored `structural` already exposes everything needed (`classes[]`, `abilities`, `proficiencyBonus`, `race`, `level`) → **no seeding / migration.**
- **NEW `resource-derive.js`** — `window.ResourceDerive.derive(structural)` returns presentation-agnostic specs `{id,label,tag,max,die,recharge,tone,source}`; `id` is the `vitals.pipState` key (`ki` / `bardicInspiration` / `superiorityDice` / `starlightStep`). The sheet's `buildResources` and the orbs' `resourceDefs` both call this one fn. **Only SPENT lives in `vitals.pipState`; current = derivedMax − spent.**
- **Grounded 5e formulas** (fetched + confirmed from the 5etools 2014 mirror, `raw.githubusercontent.com/5etools-mirror-3/5etools-2014-src/main/data`): **Ki** = monk level, short rest. **Bardic Inspiration** = max(1, CHA mod) uses; die d6→d8→d10→d12 at bard L1/5/10/15; short-or-long rest from L5 (Font of Inspiration) else long. **Superiority Dice** (Battle Master only) = 4/5/6 at L3/7/15; die d8→d10→d12 at L3/10/18; short-or-long. **Starlight Step** (Astral Elf racial, AAG) = proficiency-bonus uses, long rest.
- **Sheet wiring** (`sheet-mount.js`): new `renderResources` + `buildResources`, a "Resources" section block between Spellcasting and Features (reuses existing `.block`/`.pool` CSS, hidden when empty). **Sheet pools are DISPLAY-ONLY — the orb is the spend surface.** Host pages load `resource-derive.js` BEFORE consumers.
- **Orb wiring** (`combat-orbs.js`): `resourceDefs` merges the derived specs (subclass tone → teal `#55c4c0`, else gold `#e7c279`); derived resources auto-appear in the default loadout + config picker.
- **Honest inconsistency named to M:** leveling auto-updates Ki/Bardic/Superiority/Starlight, but **spell slots / sorcery points deliberately stay static** in the sheet's Spellcasting pane (the larger derive↔classFeatures reconciliation is a deferred Soul Shards thread).

**Player-authored custom resources — SHIPPED & DEPLOYED.** A universal catch-all so any homebrew / item / racial charge is player-addable (Vesperian's Shadar-Kai *Blessing of the Raven Queen* was the trigger case).
- **`structural.customResources`** = `[{id, label, max, recharge}]`, where `max` is a formula token resolved against the sheet (`{type:'fixed',value}` | `{type:'pb'}` | `{type:'level'}` | `{type:'mod',ability}`) so it **auto-updates on level-up**; `recharge` ∈ `short`|`long`|`short-long`. `resource-derive.js` resolves + merges them (`source:'custom'`); the sheet renders them automatically.
- **Orb config** got a "Custom resources" section: existing list with delete (`data-cdel` / class `torb-cdel` — kept distinct from the `.torb-rm` loadout handler) + an "+ New resource" form (name, max-type select with conditional number/ability inputs, recharge). Create → append + add to loadout → `commit()`. Styles in `combat-sheet-float.css` (`.torb-cres-*`). Mock `mock-custom-resource.html` approved ("This is amazing").
- **5etools racial-resource scan** (recorded for the deferred curated layer): 106 races/subraces carry a recharge-gated trait, 125 total; ~60 parse cleanly to (uses, recharge), the rest are spell-grants (≈1/long) or false positives (elf Trance, gnome Sentry's Rest). Shadar-Kai prints twice: MToF = once/long, MPMM = PB/long. **DECISION:** hybrid — custom resources are the catch-all now; a one-time curated generated racial table is the deferred convenience layer.

**Token-orb sizing + single-ring dismiss — FIXED & DEPLOYED.** Orbs scale to the token disc via a `--torb-d` variable (width/margin/font all `calc`/`em`); `hide()` removes **all** `.torb-layer`s; `bloom` strips any other token's layer first (no pile-up on re-select). `layoutLayer(layer, disc)` does sizing + ring positioning and is reused for live resize.

**Orb size sliders — SHIPPED & DEPLOYED** (M's idea: "put the settings in the token orb settings"). A **"Display size"** section in the orb config: **Orb** (fraction of disc, 30–78%) and **Sheet** (sheet orb relative to the others, 70–160%). Global prefs persisted to localStorage `tok.orbSize` with an **in-memory `_size` cache** so they work live even when localStorage is blocked (e.g. opening the preview as a file). Sliders live-resize via `layoutLayer` (no re-render flicker).

**THE BIG ONE — the `.sheet` class collision (the "massive sheet orb" that survived four fixes).** The sheet orb rendered ~3× everything else on the map, consistently, on every token. I chased it as a sizing bug across multiple rounds (CSS var `--torb-sheet-d`, then inline sizing) — **all wrong, because the width was never the problem.**
- **The diagnosis that cracked it** (M ran a console one-liner I supplied): orb **computed width = 67px** while **`--torb-sheet-d` = 14px**. The variable *was* being honoured (14px) — something was *inflating the box*, not setting the width. (Also confirmed: served CSS + JS were the correct committed versions; no service worker; no cache-buster on the asset links.)
- **Root cause:** the sheet orb's class was `torb sheet primary`. That bare **`sheet`** matched **`sheet-mount.css`'s `.sheet` rule — the character-sheet *PANEL's* class** (combat.html loads sheet-mount.css), which carries `padding: 30px 32px 22px` (64px horizontal) **and** `filter: hue-rotate(var(--ac-hue))`. 14px content + 64px injected padding ≈ the 67px. A pure class-name collision between the orb and the sheet panel.
- **Fix (one line, `combat-orbs.js`):** stop emitting the colliding kind class for the sheet orb — `kindCls = (d.kind === 'sheet') ? '' : d.kind`. The orb is styled by `.primary` regardless, and **nothing uses `.torb.sheet`** (the CSS only styles `.torb.hp` + `.torb.primary`), so it loses nothing. Diff vs live = exactly that line. **Regression guard added** (`smoke-combat-orbs.mjs`: the sheet orb must not carry a `sheet` class).

**Files this session (all staged in `/mnt/user-data/outputs/`):** `resource-derive.js` (NEW), `sheet-mount.js` (Resources section), `sheet-v2.html` (loads `resource-derive.js`), `combat-orbs.js` (derived + custom + sizing + sliders + **the collision fix**), `combat-sheet-float.css` (orb sizing + custom-resource form + size-slider styles), `preview-combat-orbs.html`, `mock-custom-resource.html`, `smoke-sheet-resources.mjs`, `smoke-orbs-custom.mjs`, `smoke-combat-orbs.mjs`. **Pending deploy: `combat-orbs.js`** (the one-line collision fix) + hard-refresh.

## This session (June 20–21) — the access-architecture pivot: the **right rail**, the **HUD/feed split**, **combat-as-state**, and the decision to **build the mountable sheet** (+ Phase-2 **inspiration shipped**, the **ToK skill**)

**Tooling — the ToK skill + the tyros fix (done).**
- Built & installed a reusable Claude Skill **`trials-of-kirtas`** (working rules, architecture spine, shared-module map, Supabase gotchas, validation harness, page + Soul-Shards maps, roster). It replaces re-uploading this doc to spin up a session. Repackaged after four corrections: `sheet-v2.html` is hand-maintained (not generated); the full-column write idiom; `items2.js` is a Netlify item-proxy function; the live-only / over-permissive `characters` RLS policy flagged.
- **Killed the recurring "tyros cleanup" hallucination at root.** It came from a stored memory edit that ended in a standing imperative ("update characters.js…") — a *completed* TODO re-injected every session. The Cosmere rename is DONE/verified (2026-06-01). Fixed the memory + inoculated the skill. **Do not resurface it.**

**Phase 2, first slice SHIPPED — the inspiration toggle (the write-path proof). DEPLOY PENDING.**
- Files: **`sheet-actions.js`** (NEW), **`sheet-v2.html`** (MODIFIED), **`smoke-sheet-actions.mjs`** (NEW, commit beside `smoke-sheet-data.mjs`). 24/24 smoke, `node --check` clean, inline boot untouched. Deploy with bare filenames.
- Layout: the Hit-Dice **control cluster** (short-rest / **inspiration** / long-rest "moons" — icon-only, hover-label on desktop, always-labelled on touch via `@media (hover)`), attunement moved into the **Status** block as ◆◆◇. Approved mock = `mock-inspiration-cluster-B.html` (the visual proxy; the real sheet won't render standalone — needs Supabase/session/nav). Rest moons are **placeholders** (rest logic later).
- **WRITE IDIOM (locked):** `CharacterData.save(key, patch)` does a **FULL-COLUMN `.update`** → always copy-merge the whole column: `{vitals: Object.assign({}, vitals, {inspiration: next})}`; a partial wipes hp/conditions. `party.html`'s HP save is the reference. Optimistic flip → reconcile to the returned row / revert on throw; `canEdit`-gated.
- **BUILD-PROVENANCE (locked):** `sheet-v2.html` is HAND-MAINTAINED, not generated; its inline boot is the live render and has diverged past `sheet-data.js` (a decoupled smoke-tested twin **not** used by the page). Edit `sheet-v2.html` directly.

**The access-architecture pivot — agreed direction (the big discussion).** "Make the sheet usable in play" resolved into a clean model:
- **The combat log = the shared `feed` table** (Supabase realtime; channels `combat`/`chronicle`; **`feed-bridge.js` is the cross-page writer**; any authenticated user posts non-hidden; only staff post hidden / edit / delete). The HUD's rolls already flow there.
- **Two write targets:** character STATE → `vitals` via `CharacterData.save`; the LOG → `feed`. Some actions hit both (cast = spend a slot + post a line).
- **THE RIGHT RAIL** (agreed; mock **loved** — `mock-right-rail-v1.html`): a docked right-side **slide-out panel mounted on every page by a shared script** (like the HUD overlay), **tabbed** — Feed / Sheet / Codex / Settings. **Feed first.** v11/Metaphor themed; holds settings/codex too. Feed pane = channel toggle + roll-mod pills + actor-colored rows + a hidden DM row + composer (mirrors `combat.html`'s feed).
- **THE HUD stays a separate top-right floating toggle** (NOT a rail tab) — a different *kind* of UI (personal action bar vs content shell), and HUD + feed must be visible at once (the rail shows one pane).
  - **Strip the HUD's "Roll History" panel** — it's the redundant mini-feed AND it sits bottom-**right**, colliding with the rail. **Keep the bottom-left action panel** (the HUD's purpose: tap your attack → roll). **Add a compact, collapsible feed TAIL** bottom-left under the action panel (combat channel, read-only, last few events; auto-collapse when the rail is on the Feed tab). The full feed + composer + channels live in the rail. On a roll the HUD only flashes a quick confirm — not a second readout.
- **Combat is a STATE, not the HUD** (DM-triggered = the encounter row going active). Surfaces REACT to it (HUD grows its initiative/turn strip; feed `combat` channel goes live; tokens get initiative). HUD on/off = personal; combat on/off = the DM's. Refinement: the HUD can **auto-wake** when the DM starts combat. DM's start/end + advance-turn = a DM-only control in the HUD; heavy tactical (tokens/fog/initiative order) stays on `combat.html`.
- **The roll ENGINE is shared.** Rolling from the sheet is the **sheet's own action** (NOT "through the HUD"); both the sheet and the HUD call one shared roller — to be **lifted out of `battle.js`**. The sheet is a first-class roll surface; the HUD is a peer, not a layer above it.
- **Sheet + feed together needs NO mount on the sheet's own page** (sheet = main area, feed = rail = the Foundry/Roll20 layout). **The MOUNT is specifically for showing the sheet on *other* pages** (floated over `combat.html`'s map). For full sheet + live map + feed at once → **the mount is the one way** (no-mount = duplicating the sheet, rejected; otherwise you get two of the three on a screen). The HUD already covers "character (combat slice) + map + feed" no-mount; a sheet-preferring player can fight from their sheet page (+ feed), rolls landing in the shared feed.

**DECISION: build the mountable sheet** (M's call — most flexibility/freedom; inevitable; stop bending the design to dodge it).
- **M1 (immediate — the foundation; source already read, approach approved in principle, confirm at session start):** extract **`mountSheet(container, key)`**. **The render layer is ALREADY root-scoped** — `renderSheet(root, char)`, `renderAbilities(root,…)`, `renderSpellcasting(root,…)`, etc. all `root.querySelector`; the boot just calls `renderSheet(document, …)`. And `sheet-actions.js` already takes a `root`. So it's tractable, not a teardown. Pieces: (a) move the sheet's body markup → a reusable template in a NEW shared **`sheet-mount.js`**; (b) root-scope the last two document-globals — `applyExtras` (portrait glow) and `showError`; (c) `mountSheet` = load → stamp template into container → `renderSheet(container,…)` → `applyExtras(container,…)` → wire inspiration scoped to container; (d) `sheet-v2.html` becomes a **thin caller** (empty mount point; boot + inline markup removed); (e) **share the sheet's CSS + the `#rough` SVG defs** as a stylesheet other pages load, so the sheet renders when floated over `combat.html`. **Parity-validated** (sheet page renders the same after; before/after diff + the smokes).
- **M2:** the **floating sheet surface** — summon the sheet as a draggable window over any page (the combat map especially), **token-linked**, DM-vs-player. **Mock-first.** (The mounted sheet / Sheet-in-rail tab gets a composer + tail at the bottom, for when the rail itself is showing the sheet so the feed isn't beside it.)
- **Folding in alongside:** the **RAIL (feed)** build, and **lifting the roll engine out of `battle.js`** so the sheet rolls.

**Mock / output files this session** (in `/mnt/user-data/outputs/`, design records): `mock-inspiration-cluster-B.html` (approved inspiration layout), `mock-right-rail-v1.html` (approved rail).


## This session (June 19–20) — Soul Shards **P6/P7**: the spellcasting **contract**, the at-a-glance sheet's Spellcasting pane **wired to data**, the **multiclass merge**, the **derive**, and the **Forge wired to it**

Continues the June 18 evening block below (which ended at "NEXT: P3 species"). P3 species (the `loadRace` loader + racial ability bonuses in production) landed in the interim; **this session built the spellcasting half of P6 + the derive half of P7, and wired the Forge to produce the real `structural`.** Two threads met at one contract: **Soul Shards WRITES `structural.spellcasting`; the at-a-glance character sheet READS it.** All artifacts in `/mnt/user-data/outputs/`, none in the repo yet. Subject character throughout: **Cosmere Runestar** — Warlock 2 (The Hexblade) / Sorcerer 1 (Shadow Magic), Astral Elf (AAG), L3, CHA 17.

**THE SPELLCASTING CONTRACT (the keystone — what Soul Shards emits, what the sheet renders).** `structural.spellcasting = { ability, saveDC, attackBonus, prepared (false→"Known" / true→"Prepared", or castType:'Mixed' when multiclass casters differ), pools:[{label, badge, tone('class'|'subclass'|'dim'), current, max, recharge}], featNote (''→hidden), groups:[{heading, spells:[{name, origin('class'|'subclass'|'race'|'feat'|'expanded'), source(tag text), time}]}], detail:{name,school,cast,range,components,duration,concentration,body,higher}|null }`. Plus `structural.classes = [{name, level, subclass}]` (the multiclass spine the sheet's subline already consumes). **Token colors** (sheet, *Metaphor: ReFantazio* aesthetic — Playfair Display / EB Garamond / Oswald): origin `--o-class:#e7c279` gold · `--o-subclass:#55c4c0` teal · `--o-race:#e0584a` red · `--o-feat:#ab92dd` purple; pool tones class=gold / subclass=teal / dim=greyed.

**READER side — `sheet-data.js` (folded, committable drop-in for the repo file; the at-a-glance / "Metaphor" sheet thread, also tracked in `CONTEXT-sheet-and-appearance.md`).** Added `poolHTML/spellHTML/groupHTML/detailHTML/renderSpellcasting` (reusing the file's own `esc`/`sgn`), one line in `renderSheet` (`renderSpellcasting(root, s.spellcasting||{})`), `spellcasting` in `SAMPLE.structural`. Diff vs repo = only those regions; `mountSheet` byte-untouched. The Spellcasting pane (authored in **`mockup-sheet-visual-direction-v11.html`** = v10 + data hooks: `data-list="pools"`, cast-meta `data-f`s, `data-list="spellGroups"`/`"detail"`) now renders entirely from `structural.spellcasting`. **84/84 smoke** — `renderSheet(SAMPLE)` reproduces the pane value-for-value.

**PRODUCER side — `soul-shards-spellcasting.js` (the multiclass merge; the hard one).** `deriveSpellcasting(input)` → `structural.spellcasting`; `deriveClasses(input)` → `classes[]`. **Computes BOTH pools itself** (robust against the data layer's pact-slot gap): **pact slots** from the Pact Magic table by Warlock level (short rest, SEPARATE pool); **shared slots** from the COMBINED multiclass caster level via the hardcoded multiclass slot table (long rest). `casterContribution`: full=L, ½=⌊L/2⌋, ⅓=⌊L/3⌋, artificer=⌈L/2⌉, pact=0. Single non-pact caster → pool labeled "{Class} Slots"; multiple → "Spell Slots". `castMeta` collapses to one DC/atk row when casters share ability (Cosmere = all CHA), else `castType:'Mixed'`. Spells are a flat **provenance-tagged** list (the picker's output) bucketed by level. Class resources (sorcery points) ride in via `input.extraPools`. **15/15 smoke** (Cosmere == the contract; generalizes — pact L2/5/11, Paladin6/Sorc1, pure-Warlock pact-only).

**PRODUCER side — `soul-shards-derive.js` (P7 derive, preview half).** `deriveStructural(input, {engine, spellcasting})` → `{structural, _incomplete:[]}`. Runs `engine.build()` per class, folds the merge, **folds race traits from a `loadRace()` model**. Spine: name, classLabel ("Warlock 2 / Sorcerer 1"), subclass label, `classes[]`, level, proficiencyBonus, abilities `{score,mod}`, **saves from the FIRST class only** (2014 multiclass rule — Warlock WIS/CHA in, Sorcerer CON dropped), grouped hit dice ("2d8 + 1d6"), combat{initiative, spellSaveDC, spellAttackBonus, **speed (race walk), senses{darkvision} (race)**}, features (origin-stamped — below), spellcasting (the contract). **NO write.** Deps injected or read off `window`. `input = {name, alignment, portrait, abilities(FINAL post-racial), classes:[{model, level, subclassShortName}], race(loadRace model), subraceName?, background?, spells?(picker output — empty→flagged), extraPools?, detail?, hp?}`. **23/23 smoke** (spine + multiclass saves + Astral Elf speed 30 / darkvision 60 / Fey Ancestry as `race:Astral Elf` + spellcasting == contract + honest `_incomplete[]`). **Bug caught & fixed here:** features now carry the engine's **origin stamp** (`f.origin` = `'class:Warlock'` / `'subclass:The Hexblade'`, race traits `'race:Name'`) — the `type:label` the sheet's feature tags parse — not the raw book source (`'PHB'`).

**`shards.html` — the Forge WIRED to the derive.** Three surgical edits (all assertion-checked): (A) two `<script>` tags — `soul-shards-spellcasting.js` + `soul-shards-derive.js` — after the engine; (B) `buildStructuralPreview` no longer assembles inline — it hands the chosen class model + level + subclass shortName + `effectiveAbilities(race)` + the `loadRace` model to `SoulShardsDerive.deriveStructural` and returns the real `structural` (+ `_incomplete`); (C) `forgePreview` drops its now-redundant pre-build, calls the 2-arg preview. **Single-class path** exercised (the builder picks one class today — the merge's multiclass machinery waits for multiclass selection UI, its own later step). Inline script `node --check` clean; **18/18 headless forge-wiring harness** runs the REAL `buildStructuralPreview` against the actual modules + a real Warlock model (Warlock 2 Hexblade → classLabel "Warlock 2", CHA save proficient, hit dice 2d8, DC 13/+5, race speed 30 + darkvision 60, provenance-stamped features, 1 pact pool of 2 slots, empty groups (picker pending), non-empty `_incomplete`). **Live `loadClass`→`build` against the 2014 mirror = M's preview-deploy check** (sandbox can't reach it). One leftover: **`abMod` in `shards.html` is now unused** (trivial to drop).

**The bundle (deploy together; M commits/deploys; C never pushes) — now 6 files, NO SQL, NO write:** `shards.html` + `soul-shards-data.js` + `soul-shards-engine.js` + **`soul-shards-spellcasting.js`** + **`soul-shards-derive.js`** + `character-data.js`. Forge stays **PREVIEW-ONLY** — its `_incomplete[]` honestly lists what's left.

**This SUPERSEDES the line-109 `structural` sketch in three fields:** `combat.hitDice` is now grouped ("2d8 + 1d6", not "d8"); `features[].source` is the origin stamp (`class:`/`subclass:`/`race:`), not the raw book; and `spellcasting` is the full contract above (not the engine's raw entitlement). `combat.senses{darkvision}` is now populated from the race.

**FLAGGED for M (D&D data / contract decisions):**
- **Pact slots = 2 for Warlock 2.** Repo `characters.js` Cosmere has `pactSlots {max:1}` — the Pact Magic table grants **2**. v10/v11 + the merge use 2. Likely a `characters.js` data bug (or confirm a house rule).
- **Data-layer pact-slot gap.** Repo `soul-shards-data.js` `mkSlots` reads only the 9-level `rowsSpellProgression` — it can't represent Pact Magic. The merge sidesteps this by computing pact slots itself. **M has an uncommitted "deployable `soul-shards-data.js` with pact slots + `detail` flag" in prior-session outputs** (NOT in repo).
- **Provenance is a NEW field.** `characters.js` spells carry no origin/source — the P6a picker (or P7) must supply which source granted each spell.
- **Pool labeling.** Single non-pact caster names its pool after the class ("Sorcerer Slots") vs the always-generic "Spell Slots" — M to confirm preference.
- **Senses.** The derive folds RACE darkvision (Astral Elf 60); **subclass upgrades (Shadow Magic → 120) need feature-text parsing (P4)** — flagged, not silently wrong.

**REPO ↔ OUTPUTS DRIFT (uncommitted — lives only in prior-session outputs / on M's machine):** `appearance-data.js` (the background manifest — verified correct, PENDING commit; needs one SQL: `alter table profiles add column if not exists appearance jsonb not null default '{}'::jsonb;` + GRANTs); the **P6a forge spell-picker mock**; and the **pact-slots `soul-shards-data.js`**. The next two builds (the picker UI, the write) should build on those rather than diverge — **surface them first.**

**NEXT (in order):**
1. **P6a spell picker** — the builder UI that supplies the currently-empty `spells[]` (provenance-tagged), driven by `loadClassSpellList(name,{sources})`. Build on M's uncommitted picker mock.
2. **The gated WRITE** — turn the Forge live: `CharacterData.save(key,{structural})` behind the existing staff/ownership gate. Back half of P7.
3. **Clear the remaining `_incomplete[]`** — proficiencies (class `startingProficiencies` + background + race, incl. racial choices), skills[] (+ the passives that derive from them), racial innate spells (`race.additionalSpells` → spellcasting as `race`-origin), AC/actions (need equipment).
4. **Then** the `character_snapshots` history table (append-only, one row per committed level — schema + GRANT/RLS per the locked design in the June 18 block).
- **Parallel / fallback (M's stated order: after P6/P7):** the **GEAR port** — read `netlify/functions/items2.js` first, lean EMBED (not overlay) into the GEAR pane, adapt `CharacterData.save(key,{inventory})`, then equip slots + attunement (`equipment` column). Money model: math in COPPER, whole-coin display (`valueCp` already on items2.js summarise).

---

## This session (June 18, evening) — Soul Shards builder: grant-engine + shell mock + **`shards.html` productionized** (P1–P2 + the flow + the real page)

Continues the June 18 (morning) block below — picks up at **P1** and lands the real page. **Three artifacts now in `/mnt/user-data/outputs/`, none in the repo yet:** the grant engine, the shell mock, and **`shards.html`** (the productionized builder). `shards.html` ships as a **4-file bundle** with `soul-shards-data.js` (P0, externalized out of the mock) + `soul-shards-engine.js` + `character-data.js` — deploy the four together. No deploy dependency until M commits them.

**`soul-shards-engine.js` — the level-aware grant engine. SHIPPED, 22/22 smoke vs live data.** Pure / env-agnostic (`window.SoulShardsEngine` / `module.exports`). Consumes the output of `SoulShardsData.loadClass()`; **the data layer owns all IO — the engine never fetches.**
- `build({classModel, level, abilities:{str…cha}, subclassShortName?, hp?})` → `{level, className, subclass, subclassUnlockLevel, hd, savingThrows, hp:{max,method,byLevel[]}, features[]` (origin-stamped: `originType` `'class'`|`'subclass'`, `origin` `'class:Name'`/`'subclass:Name'`)`, asiLevels[], spellcasting:{ability, progression, prepared, slots[9], cantripsKnown, spellsKnown|preparedCount}, pending[]}`.
- `levelUp({…})` → `{from, to, build, gained:{features, hp, asi, unlocksSubclass, spells}}` — a thin driver over `build` (apply for the single next level).
- Helpers: `abilityMod`, `avgPerLevel` (HP: d6→4 / d8→5 / d10→6 / d12→7), `preparedCasterLevel` (full=level, ½=floor/2, artificer=ceil/2), `isASIFeature` (detects "Ability Score Improvement"), `spellDelta` (newSlotLevels counts only 0→>0 transitions — a bug, fixed). **HP rule:** L1 = max die + CON; thereafter average-or-supplied-rolls + CON.

**`mockup-soul-shards-shell.html` — the interactive shell mock (the full builder UI). ~92KB, all `node --check` clean, M repeatedly happy with it.** Self-contained: **inlines copies** of `soul-shards-data.js` + `soul-shards-engine.js` + a **baked 2014 catalog** (`var DATA` = races/backgrounds/classes as name+source, plus class caster/prepared/subTitle/subAt/subs) for instant browse lists; **live-fetches full class models** for the engine readout. Draft persists to localStorage (`soulshards.mock.draft.v6`). Built & working:
- **Phantom-themed**, left vertical step-rail (→ top strip on mobile), emerging-character summary, Review with a structural-summary sheet.
- **Abilities (real):** standard-array / point-buy (27-pt, live counter) / roll-4d6-drop-lowest. **Array & rolls are pool-assign** — six values held separate, click a value then a stat to place it; placed values grey/strike and show their stat; click a placed value to move it; array pre-fills in stat order (reassignable), rolls start unassigned; **reroll all**. Point buy keeps steppers (8–15). Helpers: `abilSlot`/`deriveAbilities`/`makeSlots`/`ensureSlots`, `STD_ARRAY`, `roll4d6`, `pbCost`/`pbTotal`.
- **Browse lists** (`renderBrowseInto`, keyed via `ui`): search + per-source multi-filter (Sources ▾ checkboxes + Published/All/Clear; **UA off by default** via `isTestSource` `/^UA/`); **selection identity = name+source** (stored as `{n,s}` — fixes the dup-name "all Aarakocra light up" bug); query/sources/scroll persist across re-render.
- **Class:** level stepper 1–20, **level-gated subclass picker**, caster tag, **live engine readout** (real `loadClass`→`build`: HP roll/avg toggle, provenance-colored features (class=neutral / subclass=red), ASI prompts, spell entitlements, pending list). **Sidekicks filtered by name** (`excludeClass` `/sidekick/i`) → the list shows the 13 player classes (12 PHB + Artificer; Mystic stays out via UA source).
- **Background:** browse + "Customize this background" → **real pickers, all wired**: Skills (choose 2 of 18), Tools/languages (choose 2 of a baked ~32 pool), Equipment pack (select, 7 PHB packs), Feature (select, 13 PHB features). All flow into Review (e.g. "Acolyte (custom: Stealth + Arcana; Thieves' Tools + Elvish; Explorer's Pack; Shelter of the Faithful)").
- **Spells:** conditional step — folds out for non-casters; known/prepared mode driven by the real caster type.

**Architecture decisions LOCKED this session (all confirmed by M):**
- **Builder + level-up are ONE machine** — a level-aware grant engine applied over a level range. **Creation = apply grants L1→N; level-up = apply for the single next level** (a thin driver over the same engine). The builder is level-parameterized from the start.
- **Subclass timing:** a level-N character gets exactly L1→N grants, never future. Subclass selection gates on `subclassChoiceLevel` (Cleric/Sorcerer/Warlock @1, Wizard/Druid @2, most @3). A L1 Fighter must NOT pick an archetype.
- **Spellcasting:** known casters (Bard/Sorcerer/Ranger/Warlock) get fixed `spellsKnown`; prepared casters (Cleric/Druid/Paladin/Artificer) prepare from the **full class list**, count = casting-ability mod + caster level (full=level, ½=floor/2, artificer=ceil/2), swappable on long rest. **The spellbook lives on the character sheet (`party.html`), NOT the builder.** Prepared selection **persists across rests** (runtime state, not wiped). **Spell provenance** (class/subclass/race/feat origin) is stamped by the engine and surfaced by color/tag; **only the class pool counts against the prep limit** (granted extras tagged but excluded).
- **Character history → a `character_snapshots` table** (backend, append-only, ONE row per committed level): `{id uuid, character_key text → characters(key), level int, structural jsonb, summary jsonb, created_at timestamptz}`. **Keep ALL snapshots** (cheap; enables a future character **career/family tree** via an optional `parent_id` self-reference — M liked this). Snapshots capture **build state (`structural`) only, never runtime vitals.** Undo = in-flow (don't commit) or post-commit (restore the prior snapshot's `structural`). Same raw-SQL → explicit GRANT + RLS pattern as the other tables.
- **Source default:** show all PUBLISHED sources, **exclude UA** (`/^UA/`) unless toggled; **sidekicks excluded by name** (`/sidekick/i`) — source-filtering can't separate them from Artificer (both TCE).
- **Abilities methods:** array & rolls are **pool-assign** (above); point buy is the 27-point budget (8–15).

**Data probe (2014 mirror) — useful constants:** 134 races + 98 subraces (28 sources, PHB=9); 101 backgrounds (incl. a literal "Custom Background" entry); **17 class index entries = 12 PHB + Artificer(TCE) + Mystic(UA) + 3 TCE sidekicks**; 130 subclasses (Cleric=19, Wizard=13); spell-slot arrays are **zero-padded 9-element** (index 0 = L1 slots); `raw.githubusercontent.com` returns `access-control-allow-origin: *` (fetch works from `file://`). Base = `https://raw.githubusercontent.com/5etools-mirror-3/5etools-2014-src/main/data`.

**`shards.html` — BUILT & VALIDATED** (M renamed it from `forge.html`; canonical: **`shards.html`**). Productionized the approved mock by a defensive assembler (every anchor asserted, so silent drift fails loudly). ~82KB, 1065 lines, in `/mnt/user-data/outputs/`. **What shipped vs the plan:**
1. **Modules externalized** — drops the two inlined module copies; loads `<script src="soul-shards-data.js">` + `soul-shards-engine.js` + `character-data.js`. The **baked `var DATA` catalog stays inline** (cached browse index, as planned).
2. **Nav scaffold** — `#nav-root` immediately before `<script src="nav.js">` (the include contract); `.app{height:calc(100dvh - 52px)}` anchors the frame below the 52px bar so the rail + footer stay put and `.panel-scroll` is the only scroller.
3. **Self-contained Phantom CSS — `theme.css` deliberately NOT linked** (its body rules + z-9999 noise overlay would fight the page). Instead the page supplies locally the **10 vars `nav.js` reads** (Phantom values lifted from `theme.css`). **Consequence: the nav theme-switcher is inert on this page for now** — full theme reconciliation is a later pass. The mock's own `--ink`/`--gold` are untouched, so the forge body is pixel-identical to the mock.
4. **Name field** (`#charName`, always-visible in the topbar) → `draft.name`, live into the rail's "Emerging" line; clearing it resets to "Unnamed" + `.empty`.
5. **localStorage key bumped** `soulshards.mock.draft.v6` → **`soulshards.draft.v1`** (production lineage; mock drafts intentionally don't carry over).
6. **Forge = PREVIEW ONLY.** Button enabled; on the last step it runs the real `loadClass` → `build`, assembles the derivable `structural` subset, and renders it as JSON in a card with an honest **`_incomplete[]`** listing every field still to derive (AC/speed/skills/species + background grants/spell lists/feature text/…). **Nothing is written to `characters`** — the live `CharacterData.save(key,{structural})` write turns on at **P7**.

**The 4-file bundle (deploy together; M commits/deploys manually):** `shards.html` + `soul-shards-data.js` + `soul-shards-engine.js` + `character-data.js`. **No SQL this build** (no writes). `soul-shards-data.js` was **externalized verbatim out of the mock's inlined copy** so the bundle is internally consistent (the engine readout + forge preview were validated against exactly that data layer's API); if M's canonical `soul-shards-data.js` has since diverged, diff and keep M's — the only contract that matters is `SoulShardsData.loadClass(name) → Promise<model>` with `model.featuresByLevel[L]`, `model.savingThrows`, and `model.subclasses[].shortName`.

**Validation:** `node --check` clean on the inline UI script + all three sibling modules; **20/20 jsdom shell smoke** across two scenarios — fresh boot (steps render, name field wires to `#emName` + persists under the new key, clear-resets) and a seeded review-step draft with a class chosen (Forge button **enabled**, click guards gracefully with the engine absent — no throw, no write attempted). True end-to-end (`loadClass` → `build` against the live 5etools fetch) is **M's preview-deploy check** — the sandbox can't reach the mirror.

**Two gotchas pinned for the next session:**
- **The rail statically renders all six step rows including Spells**; the conditional skip happens during *navigation* via `activeSteps()`/`spellsActive()` (same as the mock). A non-caster still sees a Spells row in the rail — by design, not a bug.
- **The engine consumes `model.featuresByLevel[L]` (level-keyed map) and `model.savingThrows`** (lowercase abbrevs), both emitted by the data layer's `loadClass` — NOT a flat `features[]` / `proficiency[]`. (A hand-built model with the wrong field names throws `undefined[1]` at `featuresByLevel[L]`.)
- **Remaining build-out (each mock-first):** **P3** species-trait resolution — a `loadRace(name,source)` data-layer loader for race[]/subrace[] traits/ability-bonuses/speed/subraces (same pattern as `loadClass`) → **P4** full class feature-text display → **P5** backgrounds — a `loadBackground(name,source)` loader for real skills/tools/equipment/feature (the mock's pools are baked stand-ins) → **P6** the spellbook (provenance + known/prepared mechanics + spell metadata) → **P7** derive + review + write the full `structural` → then the `character_snapshots` history table.

**`structural` shape (for P7)** — assembled by the builder, written to `characters.structural`, rendered by `party.html`: `{name, classLabel, subclass, level, race, background, alignment, xp, portrait, appearance, proficiencyBonus (2 + floor((L−1)/4)), passivePerception, abilities{str:{score,mod}…}, combat{ac, hp, hpMax, hpTemp, speed, initiative (dex mod), hitDice ('d'+hd), spellSaveDC, spellAttackBonus}, saves{str:{bonus,proficient}…}, skills[{name,attr,bonus,prof}]×18, classFeatures{…}, actions[…], spells{cantrip:[…], 1:[…]…}, features[{name,source,desc}], proficiencies{…}}`.

**Working rules reaffirmed (Soul Shards):** read repo source before editing; `node --check` every JS / inline script before handover; **mock before layout, approve before code**; never change a theme var for a per-page issue (hardcode page overrides; reuse theme vars for new UI); the mock stays self-contained (Phantom inline) and `shards.html` reconciles to theme tokens later; write-back is `CharacterData.save`. Sourcing note still stands: SRD openly licensed, the full mirror is M's grey-area call (the table already runs on it).

---

## This session (June 18) — Saved-monsters shelf · table-driven characters (A/B/C) · Soul Shards P0

**Saved-monsters shelf (SHIPPED & deployed — M confirmed working live).** `combat.html`. The Tokens dock pane's "Saved monsters" section: staff-only `saved_monsters` table (`schema_delta_saved_monsters.sql` — RUN on live; RLS read+write on `is_staff()`, grant `to authenticated`, in the realtime publication, no subscription wired v1). ★ in the Bestiary is **additive** ("save a copy"); the star shows a variant count + green flash. Shelf rows are **renamable named variants** off a shared statblock ("Sister Garaele" off an Acolyte): ✎ opens an inline editor (name field + tag chips), × unsaves (instant, touch-visible — iPad has no hover), + Add spawns under the custom name via `dropMonster` (dupe-numbering free), CR groups (derived from the statblock), OR tag-filter chips, token art via `monsterTokenUrl` (keys off the BASE statblock so variants share art). **`dropMonster` was HOISTED out of `buildBestiary` to sibling scope and gained an optional `nameOverride`** so the shelf + bestiary spawn through one path — **don't re-nest it**. Data shape: `name` (editable display), `statblock` (full blob; its `.name` is the immutable base), `tags text[]`. CR is never stored (derived). 20/20 jsdom smoke.

**Table-driven characters — #1b + FK + client (BUILT & VALIDATED against real Postgres; deploy-ready).** Closes the last `characters` carryover; makes 5th/6th PCs (and future NPCs) assignable everywhere.
- **A (`schema_delta_characters_1b.sql`):** tightened `characters_party_update` from `using(true)/check(true)` → `is_member()` (the profile-less-Guest write-hole, combatants' twin). Validated on a throwaway PG16: guest UPDATE 1→0, member still 1.
- **B (`schema_delta_characters_fk.sql`):** dropped `profiles_character_key_check` (the hardcoded 4); added FK `profiles.character_key → characters(key) on delete set null`; recreated `set_membership` to validate the key against the **table** (FK is the backstop). Validated: FK rejects bogus keys, `set_membership` errors cleanly on unknown, real keys work. (`characters.key` confirmed PK; `characters` policies are live-only: `characters_read` open SELECT, `characters_party_update`, `characters_staff_all`.)
- **C (client, deploy-ready):** `admin.html` loads `character-data.js` and sources its character dropdown from `CharacterData.loadParty()` (hardcoded 4 = fallback); `combat.html` Party shelf + `addPartyToken` source name/hp/portrait from the table (`CHARACTERS` = fallback). 6/6 smoke. **Caveat:** a brand-new PC's BOARD token art falls back to a letter-disc until `battle.js` `portraitFor`/`charFor` are table-fed (separate pass); the original four keep their `PORTRAITS` art everywhere.
- **Deploy set (any order; both SQL idempotent):** `schema_delta_characters_1b.sql`, `schema_delta_characters_fk.sql`, `admin.html`, `combat.html`. **Repo reconciliation (housekeeping):** `schema_v1`'s `character_key` CHECK + `schema_delta_members`'s `set_membership` are now superseded by the deltas.
- Phase D (in-app character CREATION) was reframed → it became **Soul Shards** (below), not an admin skeleton-row form.

**Soul Shards — the character builder (NEW multi-session arc; "mock-heavy").** A real charactermancer (race/class/background/spells → full sheet), modeled on how Plutonium consumes the 5etools JSON. **Lives on its OWN page** (M named it **Soul Shards**; route TBD, e.g. `forge.html`/`soul-shards.html`) — NOT in `party.html` (a wizard ≠ the sheet viewer). It WRITES the assembled `structural` into the `characters` table via the write-back layer; `party.html` renders it. The spellbook falls out of the same data.
- **Architecture (researched):** 5etools is static JSON (no REST API) on the mirror we already use (`5etools-mirror-3/5etools-2014-src`). Class files reference features as `Name|class|source|level` strings (+ `{classFeature, gainSubclassFeature}` objects mark subclass-pick levels); spell→class linkage is the reverse index `spells/sources.json`; slots are `classTableGroups[].rowsSpellProgression`; feature text carries `{@tag}` markup (reuse `monster-actor.js`'s parser). Plutonium = a JS layer resolving these onto a sheet; its charactermancer is Patreon-only.
- **P0 data layer SHIPPED & PROVEN (`soul-shards-data.js`):** env-agnostic (window.SoulShardsData / module.exports). `loadClass(name)` → normalized model `{hd, savingThrows, subclassChoiceLevel, subclassTitle, spellcasting{progression,ability,cantripsKnown,spellsKnown,prepared}, slotsByLevel, featuresByLevel, subclasses[{shortName,featuresByLevel}], startingProficiencies/Equipment, multiclassing}`; `loadClassSpellList(name,{sources})` → class spell list via the reverse index, **source-allow-list ready** (the spellbook's filter). 19/19 live smoke (Bard known + Wizard prepared; subclass timing L3 vs L2; L9 slots `[4,3,3,3,1…]`; 120 Bard PHB spells). **First real branch = known vs prepared casters** (Bard has `spellsKnown[]`; Wizard flagged `prepared`). No deploy dependency (nothing references it yet).
- **Phase plan:** P0 data ✓ → **P1 shell+flow mock** (Abilities→Species→Class→Background→Spells→Review; nav; draft persistence) → P2 Abilities → P3 Species (`races.json`, same resolver pattern) → P4 Class (+subclass timing) → P5 Background → P6 Spells (known/prepared prep math; feeds spellbook) → P7 derive+review+save `structural` → P8 level-up/advancement (long tail). Each mock-first. **Sourcing note:** SRD is openly licensed; the full mirror is the grey area (M's call; the table already runs on it). **[P1–P2 + most of the flow are DONE — see the June 18 evening "Soul Shards builder" block up top. NEXT is `shards.html` (the real page).]**


## Current state (combat ↔ sheet sync — Option A convergence + single-source turn engine · June 15–16)

### This session (June 15–16) — write-back full-drive, ONE backend, ONE turn engine

**Write-back layer (done).** `CharacterData.save(key, patch)` (EDITABLE whitelist; update+select, empty result = RLS block → throws). STORY→NOTES editable. **Vitals full-drive** so HP, temp HP (`hpTemp`), temp max HP (`hpBonus`), slots (`pipState`), concentration, and conditions sync sheet ↔ HUD ↔ map.

**HP-mirror trigger (proven).** `schema_delta_vitals_hp_sync.sql`: bidirectional `combatants.hp` ↔ `characters.vitals->>'hp'`, loop-safe (`IS DISTINCT FROM` echo guards), PC-only (side='party' + source_key), active-encounter-scoped, `SECURITY DEFINER`. Uses `jsonb_set(coalesce(vitals,'{}'),'{hp}',…)` so it PRESERVES the other vitals keys. **Design lock:** HP is the ONLY thing combat "owns"; temp HP, hpBonus, slots, concentration live SOLELY in `characters.vitals`, NEVER mirrored.

**Realtime on characters (run).** `enable_characters_realtime.sql` (publication + replica identity full). **Líadan portrait (run).** `update_liadan_portrait.sql` + `PORT_POS={liadan:'center 18%'}` framing.

**Two-level keyboard nav (party.html).** Arrows no longer switch characters (M's accidental-switch pain): rail ↑↓; `→` enters section content; content ↑↓ walks rows; `←`/Esc back to rail; switch only via Esc→wall, dots, `[`/`]`.

**OPTION A — one shared backend, both pages (done).** `combatants-backend.js`: `window.makeCombatantsBackend(deps)` — ONE backend `combat.html` AND `party.html` install via `window.__battle.useBackend(...)`. Write semantics (hp→combatant + full vitals→characters, conditions, initiative, load-merge, event text) live ONCE; each page injects glue (combat.html: `hudComb` resolve, its realtime channel via `onSubscribe`→`HUD_ONCHANGE` so its realtime handler is UNTOUCHED, logEvent, renderInitStrip; party.html: own combatant cache + `sheet-combatants:` channel → `_emit`, CharacterData for vitals). party.html context chooser swaps resting-vitals backend ↔ combat backend on active-encounter membership. **combat.html MUST `<script src="combatants-backend.js">` after battle.js** (the easy miss — verify on deploy).

**Single-source turn engine (proven).** `advance_turn.sql`: `SECURITY DEFINER` fn both surfaces call via `rpc('advance_turn',{p_encounter})`. Computes "next" from the COMPLETE roster (definer bypasses RLS — a player's sheet can't see hidden monsters, so client-side turn math is unsafe). Order matches combat.html exactly (in_combat + rolled; initiative DESC, party-first, name ASC); wrap → round++. **Server-side off-turn guard** (staff always; player only when the active combatant is theirs — `raise 'not your turn'`). Logs the ONE replay turn event server-side into `feed`, masking hidden non-party foes to `???`. combat.html `advanceTurn()` now calls the rpc (encounters echo ~2213-2219 redraws strip+HUD); its dead name-tiebreak comparator fixed to match the SQL; `setActive` kept for retreat/start-combat only (no double-log). party.html sheet backend gained `advanceTurn → rpc`. Both pages `node --check` clean; function proven in Postgres (order, wrap→round++, hidden-in-cycle, single masked event, off-turn guard).

**✅ DONE this session (was the gap): editable HP system ported into party.html.** Lifted `sheet.html`'s vitals editor into party.html STATS, rewired to `CharacterData.save(key,{vitals})` (+ `structural` for Max HP edits). Segmented HP bar (main green `#5a9a6a` → bloodied `#d4821a` → critical `#c8432a`, **temp HP gold, temp-max/bonus blue `#5a9aaa`** — sheet.html's real mapping; M's remembered blue/gold was reversed) + damage/heal, Temp HP / Bonus Max / Max HP steppers, short/long rest. Saves debounced 300ms (sticky structural flag so a Max HP edit can't be dropped), merges vitals so `pipState`/concentration survive, optimistic bar + realtime reconcile, gated on `c._canEdit`. 18-check jsdom smoke passed. **Any-party editing also done:** `characters_party_edit.sql` (RLS `characters_party_update using(true)` + identity-pinning `characters_guard_columns`; staff bypass) — proven under real RLS (`SET ROLE authenticated`): a non-owner edits an absent player's vitals/structural, owner/key stay pinned, staff can reassign owner. `canEdit` widened to `!!me.role` (any member). NOT an RLS block was the lesson (the HUD always wrote fine); the sheet just lacked the UI.

**Files (DEPLOY PENDING — run SQL first, then push pages together):**
- Run + fold into `schema_v1.sql`: `schema_delta_vitals_hp_sync.sql`, `enable_characters_realtime.sql`, `update_liadan_portrait.sql`, `advance_turn.sql`, `characters_party_edit.sql` (any-party edit RLS + guard).
- Push together (pages depend on the new module + rpc): `character-data.js` (write-back + `canEdit` widened to any-party), `combatants-backend.js` (NEW), `combat.html`, `party.html` (now incl. the editable HP widget in STATS), `characters.js`.
- Smoke: map End Turn advances + logs once + strip redraws; sheet player ends own turn, blocked off-turn; HP/conditions/initiative/slots from sheet HUD in an active encounter → lands on map; realtime cross-surface; Líadan framing; arrows can't switch characters.

---

## Previous arc — Character-sheet + party-page (Supabase `characters`, read-path, `party.html` takeover — Supabase `characters`, read-path, `party.html` takeover · June 13–14)

### This session (June 13–14) — the character sheet rebuild

**Phase 1 — game-menu mockups (approved).** Three iterative interactive mockups of a full-screen "game menu" character sheet, fusing FF13 (full-height portrait wall), Days Gone (diagonal monochrome ink + huge sparse type), and Metaphor (stacked oversized menu words). Locked: `mockup-party-menu-v3.html` (outputs). Decisions — one unifying **master-detail "detail drawer"** (bottom strip, consistent across all sections); **GEAR absorbed as a full workspace** (equipped body-slot paper-doll + carried grid + 5etools search overlay + item detail), not a separate route; **attunement = independent 3-pip tracker** (handles "staff or sword in a hand slot"); type-aware equip (tap→EQUIP mobile / drag→slot desktop); FEATURES as a level/source synthesis (not a full tree); sections character-aware (SPELLS hidden for non-casters); players may edit their own ability scores (ASIs/item bonuses are legit); Notes lives inside STORY.

**Phase 2 — characters in Supabase (run & verified live).** `public.characters` (`schema_delta_characters.sql`) — see Architecture/Live-DB. `migrate_characters_data.sql` seeded all 4 (structural from `characters.js`; vitals/inventory/currency/bio/notes from the JSON).

**Phase 3 — export backstop (working, after a long debug).** Mirrored the feed-export trio: `lib/characters-export-core.js` + `characters-export.js` (HTTP, staff-gated, clean final version) + `characters-export-nightly.js` (`30 10 * * *`). Supabase = live truth, git JSON = durable backup. The two root causes that ended the saga are pinned under Live DB / env state. Final: `200 {committed:4}`. *(M pushed back twice on C's wrong key-format diagnosis — both times correctly; the decode + the `42501` hint were what cracked it.)*

**Phase 4 — read-path + `party.html` takeover (DEPLOYED, read-only v1).** `character-data.js` reader (see Architecture). `party.html` REBUILT as the approved menu — portrait wall → in-page sheet (STATS / SKILLS / FEATURES / SPELLS / GEAR / STORY), detail drawer, character switcher, keyboard nav (↑↓ section, ←→ character, Esc party) — themed via `theme.css` tokens (rides the active theme; Phantom ≈ the mock), live data, preserving `#nav-root` + includes. **Looks great in M's screenshot.** Nav-bleed fixed: `.pm` offsets below the 52px fixed `#site-nav` (default `--pm-top:52px` + dynamic re-measure on `nav:ready`/resize).

**Read-only boundary (deliberate).** v1 is a *viewer*. GEAR shows the carried list + currency only — the **equipped-slots paper-doll is empty because no equip *state* exists yet** (`equipment` col is `{}`; the mock's filled slots were hardcoded). Slots, attunement, container editing, and 5etools-add all share one blocker: **they write data, and the page can't write yet.** FEATURES groups by `source` (data has source, not level-gained); the skill drawer uses a bundled static D&D rules-text table (data has bonuses, not prose).

**Data shapes (for the write-back build):** `structural` = `{name, classLabel, subclass, level, race, background, alignment, xp, portrait, appearance{…}, proficiencyBonus, passivePerception, abilities{str:{score,mod}…}, combat{ac,hp,hpMax,hpTemp,speed,initiative,hitDice,spellSaveDC,spellAttackBonus}, saves{str:{bonus,proficient}…}, skills[{name,attr,bonus,prof}]×18, classFeatures{pactSlots:{current,max,level}…}, actions[…], spells{cantrip:[{name,castingTime,range,duration}],1:[…]}, features[{name,source,desc}], proficiencies{…}}`; `vitals` = `{hp,hpTemp,hpBonus,pipState{…},concentration}`; `inventory[]` = full 5etools objs; `currency` = `{pp,gp,ep,sp,cp}`; `bio` = `{personality,ideals,bonds,flaws,backstory}`.

**Files (HOUSEKEEPING — commit not yet confirmed):** `schema_delta_characters.sql` (incl. service_role grant), `migrate_characters_data.sql`, `grant_service_role.sql`, the 3 export functions, `character-data.js`, `party.html`. Then fold the `characters` table + RLS + guard + grants into `schema_v1.sql`. (Outputs also hold `HANDOFF-party-sheet.md` — a one-file mirror of this arc; keep or discard, M's call.)

---

## Previous arc — UX audit (calibration UX, tabbed dock + ticker, mobile triage, dblclick chip + hints · June 12)

### This session (June 12) — the UX audit arc

**Round 1 — calibration UX (deployed & VERIFIED):** (1) `buildBoard` preserves the user's zoom/pan when rebuilding the **same map image** (`boardEl.dataset.scene` vs `META.publicId`; `fitBoard(meta, keep)`) — fixes the fit-reset on every cal nudge, span preview, Reset, the post-save realtime echo, and other-staff recalibrations; real scene switches/resize/fit-button still reset. (2) Calibration **tool toggle**: ⊞ Span (default each entry) / ✋ Pan chips in the cal bar (`CAL.tool`, `calSetTool`); Pan mode + **Space-hold** (`CAL.space`, input-focus-guarded) make the capture listeners step aside so the normal grab/pinch machine works mid-calibration; `.cal-pan` grab cursor. Marker: `calSetTool`.

**Round 2 — tabbed dock + roll ticker, UX-AUDIT-1 (deployed & verified):** see Architecture. Built from the approved `mockup-tabbed-dock.html`. jsdom smoke suite covered tabs/collapse/ticker/unread/masking.

**Round 2b — tab overflow fix, UX-AUDIT-1b (deployed & verified):** five labeled tabs overflowed 320px and pushed ✕ off-screen (mockup said FEED; production title was 'Game feed' — C's miss). Options mocked at true width (`mockup-tab-fix.html`); M picked **A: icon-over-label** over pure hover-icons (C pushback: iPad has no hover; labels were the audit's point).

**Round 3 — mobile triage MOBILE-TRIAGE (deployed; v2 fix in hand):** stage `100dvh` (vh fallback), `interactive-widget=resizes-content` viewport meta, dock = full-screen sheet on phones (✕ returns to board), `--kb` CSS var from a `visualViewport` listener so the composer rides above the iOS keyboard (feed re-pins to newest). **v2 (this handover, awaiting deploy):** the breakpoint is now `(max-width: 640px), (max-height: 500px)` — landscape iPhones are ~900px WIDE but ~430px tall, so the width-only query missed them and the composer was cut at the screen edge (M's screenshot). Landscape remains cramped (nav + marquee eat ~130px of ~430) — livable for triage; the mobile audit owns the real fix.

**Round 3 same files — UX-AUDIT-2 (deployed; verification pending):** (1) **player dblclick on an enemy token** shows a read-only `tok-chip` (name + conditions or 'No conditions' + 'DM controls this creature', self-dismissing ~2.6s/any tap, XSS-escaped) instead of dead air; staff paths unchanged. (2) **First-use hints** (`hintOnce`, once per browser via localStorage): players get the dblclick-to-drive tip on load (`tok_hint_drive`); staff get the dropped-hidden explainer on first bestiary drop (`tok_hint_hiddendrop`). `pageToast`/`hintOnce`/`showTokenChip` are deliberately **top-level** (a scope probe caught the first placement: callers live in three different scopes — a ReferenceError node --check can't see; brace-depth probing is now part of C's toolkit for this file).

**The monster→PC "next" bug — NEEDS M'S REPRO.** C did the focused read: `endTurn` off-turn gate → `_doEndTurn` → seam guard (`advanceTurn(key)`, staff bypasses; non-staff must be driving the active combatant) → page `advanceTurn` → `setTurn` M3 block — every constructible monster→PC path reads coherent. Do NOT guess-fix. Get from M: who the HUD was driving, what was pressed, expected vs. actual.

**UX-audit walkthrough backlog (from the three-persona pass, not yet built):** ADV/DIS/BLESS/GUIDE only visible inside the feed pane (a player on another tab has no path to advantage); typed `/roll` syntax undocumented in-UI; init-strip name-click-selects unexplained; fog brush discoverability; unseat-active-combatant's silent round-tick side effect; roster pane scroll reset per turn advance (known minor).

**Session mockups in outputs (not in repo unless M committed them):** `mockup-cal-toolbar.html`, `mockup-tabbed-dock.html`, `mockup-tab-fix.html` — keep or delete, M's call.

---

### Earlier state (M3 auto-load + token menu + auras — token menu round DEPLOYED & VERIFIED by M; auto-load BUILT — **M has not yet confirmed the battle.js deploy/smoke**, ask)

**M3 final piece — NPC-turn auto-load (battle.js, this round):** when the shared tracker lands on an NPC's turn, the staff HUD auto-loads that monster, with a turn toast in enemy red. Lives in `setTurn` (the existing turn-start hook). Guards: only while the HUD is already on (never yanks it open), only on a real turn change (round-only ticks are no-ops), only `mon:` keys that resolve in the registry (player browsers never registered any — untouched), never PC turns (players drive their own; the DM isn't pulled off what they're reading). After the monster's turn the HUD stays put until the next NPC turn or a manual switch — predictable beats clever. Verified under the DOM shim: staff auto-load, player immunity, PC-turn/round-tick no-ops. Marker: `NPC's turn auto-loads` comment in battle.js setTurn. **M3 is now fully complete** (double-click → Battle HUD, menu item, and auto-load all shipped).

### This round's earlier work (deployed & verified by M — "That worked")

M deployed M1+M2 ("working pretty well") and reported via voice-to-text: (1) freshly dropped monsters didn't appear in the roster pane until revealed, defeating hidden ambushes; (2) the HUD's monster driving wasn't reachable anywhere — no picker section showing, no token interaction; (3) wanted the token context menu redesigned: useful info first, conditions demoted to an expander, and the long-discussed **aura** feature. Mid-round M renamed the action: **"Battle HUD"** (not "Drive in HUD") everywhere user-facing.

**Bug 1 (roster pane stale on own drop) — root cause:** `dropMonster` pushed to COMBS + `renderTokens()` but never `renderInitStrip()`, and the realtime INSERT echo dedupes your own drop and returns before its repaints. Fix: drop now calls `renderInitStrip()` (cascades to `paintCombatMenu` + `pushMonstersToHud`). Hidden monsters appear on the bench immediately with the ⊘ eye — seatable without revealing.

**Bug 2 (ENEMIES picker never appearing) — root cause:** `setMonsters` painted into `#b-cpick-enemies`, which only exists after `mountHud()`; on load the push ran first and silently no-opped, and nothing repainted. Fix: render extracted to `renderEnemiesPick()` (battle.js), idempotent, called from setMonsters + end of mountHud + `toggleCharPick` open. Regression-tested with a strict null-until-mounted DOM shim.

**Token → HUD interactions (M3 pulled forward — M expected them):** new public `window.__battle.drive(key)` (turns the HUD on if off, loads the actor, validates via charFor). **Double-click** any party token → that PC in the HUD (everyone — M: tokens are the table's interface, the HUD is the in-combat sheet); staff double-click seated enemy → drives it; benched enemy → opens the statblock. Desktop affordance; the menu item covers touch.

**Token menu redesign (mockup `mockup-token-menu.html`, approved with the rename):** the Conditions/Manage **tabs are gone** — one flat menu: name → info line (hp/AC for party-to-everyone, monsters staff-only; ⊘ hidden; in-the-fight) → 📖 Stat block (staff) → ▶ **Battle HUD** (party: everyone; enemy: staff, seated — benched shows a dimmed "seat them first") → staff Manage (Reveal/Hide, Roll initiative, size buttons) → **Aura expander** → **Conditions expander** (existing icon-chip rows, count badge, stays open for multi-toggle) → Remove (staff, confirm). Old `condTabBody`/`manageTabBody` deleted (dead). **Flagged behavior change:** Roll initiative moved from the everyone-visible tab into staff Manage — players roll via the HUD (their flow); revisit if anyone misses it.

**Auras (new, `schema_delta_aura.sql`):** `combatants.aura jsonb` `{r: 5|10|15|20|30|60, color}`. Rendered as `.token-aura` INSIDE the token element, painted before the disc (paint order, no z-index), sized in % of the token — follows drags, scales with creature size and zoom, hides with hidden tokens, **skipped for teased tokens** (no leak). Realtime: UPDATE merge carries `aura`, aura change re-renders the layer. Permissions: staff any token; **players party tokens only** (Spirit Guardians etc.) — enforced in the **column guard** (delta reproduces the whole guard body + `if old.side <> 'party' then new.aura := old.aura`; keep in sync with schema_v1 if the guard changes). Menu: radius chips, 6 color swatches, Remove aura; optimistic write with rollback. Uses CSS `color-mix` (fine on M's modern browsers).

**Verified:** node --check all; timing-fix regression (registry before mount → paints on open), drive() accept/reject; aura % math sanity (medium+15ft → 700% ring). Found-and-fixed in review: the Remove-aura button's `data-act` wasn't matched by the wiring selector (widened to `[data-act]`). **NOT verified live.**

### Previous round (M1+M2: monsters in the HUD — deployed, working; bugs above were the follow-up)

This session: M chose the combat-page track (chronicle "needs a bit more work" later — M's words; server browser is live and accepted after a sidebar fix, see previous round). Design locked via mockup (`mockup-monster-hud.html`). The agreed arc is **M1 → M2 → M3** (M3 still to build). Files changed: **`monster-actor.js`** (new), **`battle.js`**, **`combat.html`**. Deploy markers: `MONSTER-ACTOR-V1` (monster-actor.js), `setMonsters:` (battle.js), `hudComb` (combat.html).

**M1 — `monster-actor.js` (parser + adapter, fully unit-tested).** Turns a combatants row + its 5etools `statblock` into the exact character shape battle.js eats. Parses `{@hit}`/`{@damage}`/`{@dc}`/`{@recharge}`: attacks (hit mod, dice, crit-doubled dice incl. multi-term `2d6+1d4`→`4d6+2d4`, damage type, reach/range note), save effects (`DC 13 DEX · half on success`), versatile weapons (auto second `(2H)` action), rider damage (`+ 2d8 Poison` in the note), multiattack as a utility note, graceful degradation (unparseable → readable note, never broken). Adapter: live row hp/ac beat the statblock, `initiative` = DEX mod, `hiddenFoe` (drives 🕯 + hidden rolls), `art` passthrough, `mon:<uuid>` key helpers.

**M2 — the HUD drives monsters.**
- **battle.js**: `charFor()/colorFor()/portraitFor()` are now THE actor accessors (all 22 former direct `CHARACTERS/PORTRAITS/CHAR_COLOR` sites routed); `MONSTERS` registry + `window.__battle.setMonsters(rows)` (rebuilt wholesale; falls back to a PC if the driven monster leaves the fight); enemies sections injected into the desktop picker (`#b-cpick-enemies`) and mobile menu (`#bm-char-enemies`) — red rows, live HP, 🕯 hidden, dead dimmed; letter-disc SVG data-URI fallback when token art 404s; Spells button hides for spell-less monsters (desktop + orb); HUD rolls from a monster carry `actorName` + `hidden:hiddenFoe` into logRoll; monster keys never persist to localStorage.
- **combat.html**: `hudComb(key)` routes the whole backend seam — PCs by `source_key`, `mon:<uuid>` by row id (load/save/saveConditions/setInitiative); hp/condition/initiative **events from hidden foes log hidden:true** (`evHidden`, mirrors leave()); HUD_ONCHANGE now also forwards NPC row changes under `mon:` keys (applyCombatChange's SESSION guard makes it a no-op unless the DM drove that monster); `pushMonstersToHud()` runs inside `renderInitStrip()` (staff-only — **view-as-player passes `[]`**, so the preview is honest and players never receive monster data); `activeSourceKey()` returns `mon:<id>` for NPC turns (HUD off-turn logic recognizes "its" monster; player browsers never registered the key → harmless miss).
- **Previous-turn button (M's ask)**: `retreatTurn()` — staff correction tool on the init strip (`◂ Prev`, ghost style, left of Next ▸). Walks the order backwards; wrapping decrements the round (floor 1); unknown active → last in order. Goes through `setActive`, so it **logs a normal `turn` event** — replay data stays a plain sequence of truth. Staff-only by design (flagged + accepted).

**Verified:** `node --check` everything; parser suite (goblin/guard/drake/spider/net/multi-term); `hudComb` routing + `retreatTurn` math unit tests; battle.js smoke under a DOM shim (registry, picker render, fallback art, setChar guard, teardown). **NOT verified live** — checklist in Next steps.

**Design notes locked this round:** mockup's centered HUD was an artifact — the real HUD stays in its existing left position, untouched. Enemies list scopes to in-combat monsters only (bench stays in the Bestiary/roster). M3 will make **token clicks load the HUD for PCs too** — M: "tokens are the table's real interface; the HUD is doing the character sheet's job in combat."

### Previous round (Phase 3A: chronicle server browser — deployed; M accepted after a sidebar fix)

Post-deploy M flagged the sidebar as awkward: long real session titles broke the category headers (bare text node in the flex head wrapped mid-label — fixed with a nowrap label span + one-line ellipsized hint, full title on hover), and pre-feed sessions rendered as heavy single-child categories. Fix shipped: **a session whose only channel is #chronicle renders as ONE flat row** (`# Session 2 — Title · count`, opens directly); the category/tree treatment appears automatically once a session gains combat channels. M: chronicle "needs a bit more work" eventually — parked, not specified. Original build details:

This session built the **chronicle-page server browser** (design locked via interactive mockup, M approved). Files changed: **`schema_delta_campaign_config.sql`** (new), **`feed-render.js`** (new), **`combat.html`** (extraction only), **`chronicle.html`** (the feature). Deploy markers: `FEED-RENDER-V1` (feed-render.js), `SERVER-BROWSER-V1` (chronicle.html).

**1. `feed-render.js` extraction** — the Discord-style feed row renderer (escapeHtml/stripTags/side/nameColor/time/avatarHtml/rowHtml) was lifted out of combat.html into a shared module both pages load. The module **owns the feed avatar art** (single source of truth for the feed look); combat.html's `PORTRAITS`/`TOKENS` remain authoritative for **map tokens** — a separate concern. combat.html keeps `escapeHtml`/`stripTags` as hoisted thin wrappers (used in 31 places) and `renderFeed` uses `FR.rowHtml`. Pages provide the CSS (`feed-row` family); chronicle carries a copy scoped under `#sv-combat`.

**2. Channel tree** (chronicle sidebar, replaces the flat session list): 📖 All Entries (the untouched original reading view, default landing) → TAGS category (staff-pinned tags as channels, e.g. `#loot`) → session categories (collapsible; newest seeded open) each holding `#chronicle`, one `⚔ <encounter-slug>` channel per encounter with feed rows, and `🎲 table-talk` (combat rolls with no encounter_id, via feed-bridge). Counts exclude `kind:'event'`. Built from a **lightweight feed index** (`select id,session,encounter_id,channel,kind,hidden,created_at` — no bodies; cheap at thousands of rows) + an `encounters` fetch for names. Authors/Tags filters + staff session controls unchanged below.

**3. Combat channel pane** — encounter banner (name, date, rounds from `encounters.round`; staff additionally sees combatant count from the hidden `combat_start` snapshot — RLS keeps that staff-only by itself), **ROUND N dividers** woven from visible `turn` events (players get them too: turn events are hidden:false), paginated newest-100 with "↑ LOAD OLDER" (cursor `.lt('created_at', oldest)`, anchor-scroll preserved), hover-✕ delete mirroring RLS, feed images → the chronicle's lightbox. ▶ REPLAY button stubbed disabled — Phase 3B plugs in there.

**4. Server-side search** — debounced 300ms, `ilike` on body (`%`/`_` escaped), events excluded, 50-hit cap, stale-response guard. Scope pills: ALL CHANNELS / THIS CHANNEL (current combat channel, or current session's chronicle, or all chronicle). Hits show session/channel/date + highlighted snippet; clicking a chronicle hit opens that session's entries, a combat hit opens the channel and scroll-highlights the row if it's in the loaded page (deeper rows: channel opens at newest — accepted v1 simplification). The old client-side entry search is superseded; `activeFilters.search` machinery left dormant/harmless.

**5. Shared config (`campaign.config jsonb`, new delta)** — `{ channelOrder: {session: [ids]}, pinnedTags: [] }`. Staff-only by the existing campaign update policy; streams live via the existing campaign realtime membership. **Staff drag-reorders channels** within a session (⋮⋮ grips, HTML5 DnD; non-staff never see grips); unordered channels keep the chronological default (chronicle, encounters by start, table-talk). **Staff pins/unpins tags** via 📌/📍 toggles on the sidebar tag list. `bumpSessionTo` writes only `current_session` so config never clobbers (and vice versa).

**6. Realtime extensions** (same `chronicle-live` channel): combat-channel feed INSERTs update the index/counts and append live to an open channel (events feed the dividers); feed DELETEs prune index + open channel; `encounters` INSERT/UPDATE refresh channel names/rounds live; campaign UPDATE now also syncs `config` (pin/reorder propagate to everyone).

**Verified:** `node --check` on all JS (inline extracted), FeedRender smoke tests (party/DM/enemy/hidden rows, escaping), channel-ordering unit tests (default chronology, config override, event-excluded counts, slugs), round-divider weave test. **NOT yet verified live** — see deploy checklist in Next steps.

#### Earlier round (roster picker + events + nightly export — all shipped & verified)

Phase 2 (chronicle unification) is **live and verified**, the combat-page UX round before that too (right dock, dice tray, feed roll-modifier row, feed-bridge.js, feed delete + lightbox — details below). The latest session shipped, deployed, and **M verified working**:

**1. The roster/encounter picker** — the Combat dock pane is now the roster. Out of combat: Party/Others groups with all·none links, everyone checked by default; `PICK_OFF` (a Set of DESELECTED ids, page-local) means new bestiary drops default to checked and the bench persists across fights. Two start modes (locked design): **Start — players roll** (NPCs auto-roll, PCs pend for the one HUD prompt) and **Start — auto-roll everyone** (no prompts; also posts a public party-init feed summary mirroring the hidden NPC one). During combat the same pane shows the fight in initiative order plus an "on the board — check to join" bench: **only the checkbox** seats/unseats (a name click selects the token — anti-misclick); mid-fight joins roll NPCs instantly / seat PCs pending without touching round or turn; unseating the active combatant advances the turn first (note: if they were last in the order, the round ticks via advanceTurn's wrap — accepted side effect). `startCombat(mode)` + `seatOne`/`unseatOne` are the new lifecycle verbs. **`rollAllInitiative` is now seated-only** (re-seating the whole board would undo the picker) — strip + pane both.

**2. Off-turn End Turn confirm (battle.js)** — the hard block is now a `b-modal-box` confirm ("It's **X**'s turn — advance the shared tracker anyway?"; generic wording when the tracker is on an NPC/null). Confirming calls `backend.advanceTurn()` directly, deliberately skipping the own-economy modal. `confirmOffTurn`/`cancelOffTurn` on the public API.

**3. Phase 3 event logging is LIVE** — `kind:'event'` rows, zero schema changes (the check constraint and `result jsonb` were built for this). Conventions: `result` = machine payload `{type, ...}`, `body` = human line, actor = `System`, session + encounter stamped by `feedInsert`. Events: `combat_start`/`combat_end` (full roster snapshots — the replay's opening/closing positions; **hidden:true**, they can contain unrevealed foes), `turn` (public, hidden-foe names mask to `???` in the body — players trigger this via HUD End Turn so the row must be hidden:false under RLS), `move` (in-combat only; hidden foes → hidden:true, always staff-initiated), `hp` (HUD save seam — the only HP write surface today; future NPC HP tools should call `logEvent` the same way), `condition` (full-array snapshots, not deltas — scrub-friendly), `initiative`, `join`/`leave`. The feed UI **skips** events (load query `.neq('kind','event')` + `onFeedInsert` early-return; no unread-dot spam) — recorded, not displayed. Player-visible replay will need hidden snapshot rows handled staff-side (players never receive them under RLS — correct for live play).

**4. Nightly scheduled export** — `netlify/functions/feed-export-nightly.js`, `exports.config = { schedule: '0 10 * * *' }` (≈4am Mountain). The export core was factored into `netlify/functions/lib/export-core.js`, shared by the button (`feed-export.js`, HTTP contract unchanged: CORS/POST/staff-gate/response shapes) and the schedule — no drift. Scheduled fns aren't HTTP-routable, so the nightly path has no auth gate by design. The `unchanged` short-circuit means quiet nights commit nothing. Nightly covers the **chronicle channel only** — `kind:'event'` replay rows stay Supabase-only like dice rolls (a replay-data backstop would be a separate private artifact; repo is public).

**Known minor:** the roster pane fully repaints on every `renderInitStrip` (scroll position resets each turn advance) — fine at party scale, make repaints targeted if fights get huge.

### Previous round (combat-page UX, all live)

**1. Right dock** (see Architecture). Feed opens by default; unread dot on 💬 when rolls land while minimized; entering player view hides staff icons and falls back to the feed pane. Combat dropdown became proper pane buttons. Zoom bar moved to `right:56px` to clear the rail; dock panel stops 88px short of the bottom to clear the HUD.

**2. Dice tray** on the left tool rail: ⚄ expands a 1–8 count row + d4–d100. Count persists per die, resets to 1 on die switch. Tray + feed quick dice route through `feedRollWithMods`.

**3. Roll modifiers moved into the feed.** ADV/DIS/BLESS/GUIDE row in the feed drives battle.js `RS` via the seam — one state for HUD attacks/init AND feed button dice. d20s only; adv/dis applies to single-d20 rolls (`2d20kh1/kl1`, dropped die dimmed); bless 🙏 / guidance ✦ append annotated d4s; stored `formula` carries the `+1d4`s. **Typed `/roll` stays verbatim** (deliberate deviation — flagged and accepted). Roller popup hidden on combat page only; toast parked at `left:476px` beside the HUD.

**4. Cross-page rolls** via `feed-bridge.js` (see Architecture) — roll from sheet/world/etc. and it lands in the live feed.

**5. Feed curation + images.** Hover ✕ delete per row (renders only where RLS will allow it: staff anything; authors own non-hidden chronicle rows; combat rolls immutable for players). Realtime DELETE propagates. Chronicle images in the feed constrained (max-height 120px, `!important` to beat Quill inline widths) with the chronicle's exact lightbox.

**Fixes:** feed scroll trap (`.feed-wrap` missing from the wheel-exemption list), wheel zoom 1.1 → 1.07 (~30% gentler; pinch + buttons untouched), statblock visibility gate (the dock-rail click-eating bug).


## The big vision (agreed direction)

A **"Discord server within the site"**: one append-only event log, surfaced two ways — a compact **live hub on the combat map** and a full **"server" browser on the chronicle page** (all sessions/channels, searchable, eventually replayable). Organized by **session → encounter**, with combat rolls, chronicle narration, loot, and story all in one consolidated record. The capstone is a **chess-match-style replay**: re-watch a fight move-by-move (token positions, attacks, damage, HP), with loot linked to the fight it came from.

**Storage strategy (locked):** Supabase is the live/realtime source; the **git-committed JSON stays the durable backstop** (an export job preserves it). Rationale: dice rolls are a high-frequency firehose that must NOT be git-committed (would spam the repo + be slow); chronicle entries are low-frequency/durable and suit the file archive. Realtime inherently means a live-service dependency *for live play*; data-loss is neutralized by the export backup. Full unification of the chronicle onto Supabase is wanted, done in phases.

**Editor / rich-content layer — decision (June 24): stay on Quill for now; TipTap parked for a future "connected content" layer.** The chronicle's `@`-mention NPC/location linking is **custom code on Quill** (colored spans tagged `data-mention-type`, found by color + tracked in a `mentionMap`; `#` tags the same way) — NOT the `quill-mention` lib, so it's **reusable on any new Quill surface** (the at-a-glance sheet's **Notes** tab will reuse it). **TipTap (ProseMirror, MIT, headless) was evaluated** and is genuinely better *for a connected layer*: mentions are structured **nodes** (`{id,type,label}`), not color-spans → clickable + **queryable** refs (wiki-style backlinks — "every entry/journal/quest that mentions this NPC/location"), clean text+image+ref content, and one reference system across every surface. **Why parked, not adopted now:** ToK is no-build vanilla; TipTap's own "vanilla" path assumes a bundler, and the genuine no-build route is CDN-via-ESM — works but finicky (duplicate `prosemirror-model` → *"adding different instances of a keyed plugin"*, fixed with an **import map**); it's headless (build the toolbar yourself); and full consistency would eventually mean **migrating the chronicle off Quill**. **Revisit TipTap IF/WHEN M commits to the broader vision** — Feed→Chronicle revamp + DM/player **image submission** + in-game **journaling** + a **Quest Tracker**, all sharing one unified, queryable NPC/location reference system — at which point the one-time CDN/import-map setup amortizes across many surfaces and the structured-reference graph is the actual payoff. **First step when revisited:** pilot TipTap on ONE reference-heavy surface (journaling), prove the import-map load + a shared mention extension, *then* extend it and migrate the chronicle. **Until then: Quill everywhere, reuse the existing mention machinery.**

---

## Next steps (phased)

**Right now (top of the queue, after the June 21 resources/orbs session):**
- **Deploy `combat-orbs.js`** — the one-line `.sheet` class-collision fix (sheet-orb size). Hard-refresh after.
- **Resource follow-ons (offered, not built):** a **"take a rest → restore matching pools"** button (recharge is a *display label* only right now — easy win); the **curated/generated racial-resource table** (en-masse 5etools convenience layer so documented races auto-fill; players only hand-add homebrew / item charges); the larger **derive↔classFeatures reconciliation** so spell slots / sorcery points derive too (a Soul Shards thread).
- **Orb features still queued:** macros / favorite-attacks (needs the **roll engine lifted out of `battle.js`** first) and monster legendary-action orbs.
- **Standing from the access-architecture pivot:** the **right rail** (Feed / Sheet / Codex / Settings), **M2** (floating draggable sheet over `combat.html`), and the **roll-engine lift**.
- **Optional housekeeping:** add a `?v=N` cache-buster to the asset tags in `combat.html` (no cache-buster today → deploys can serve stale JS/CSS until a hard-refresh; not what bit us this session, but it would remove that whole class of confusion).

*(Older phased items below — the GEAR build, mobile audit, pending confirmations — still stand where not superseded.)*

---

**Immediate — the GEAR build (the big one).** Vitals editing is done; the sheet now writes HP/temp/temp-max + any-party. **This session shipped the net-worth + write-back increment** (see "This session" below): `items2.js` now emits raw-copper `valueCp`; `party.html` GEAR has a WORTH ledger line, per-card value, and a drawer **SET WORTH** / **MARK TO SELL** pair that write the `inventory` column via `CharacterData.save(key,{inventory})`. **#1 next: the embedded 5etools search** in the GEAR pane (now unblocked — `valueCp` is the number it needed). Engine logic lives in `sheet.html`; `items2.js` is the stateless 5etools search proxy. **Resolved open decision: lean EMBED, not overlay** — the old engine is heavily wired to `sheet.html`'s DOM (`_currentInv`, `CharacterStore.save`, `equip-*` IDs), and an overlay fights "GEAR is one workspace" + bridges two save layers. The keydown nav bails on focused inputs (`if (e.target.matches('input,textarea')) return;`), so a search box won't fight arrow-nav. After search: a **re-enrichment pass** to backfill `valueCp` onto existing items by name (they read "unvalued" until then), then **equip slots + attunement** (`equipment` column; the empty paper-doll). **The shopkeep stays deferred — it's a later *mode* of the same search engine (curated stock + optional currency-spend), not a separate page.** **DEPLOY pending: `items2.js` (first) then `party.html`.**

**Then — the gear build (each step stands on write-back):** (1) **Port the existing inventory engine** (`items2.js` — nested containers, drag-drop, pack auto-explode, 5etools search/add, weight rollup, locking) into the GEAR pane, operating on the `inventory` column via write-back. **Open decision: embed it as a module in the pane vs. open the existing inventory UI as an overlay — read `items2.js` first to judge how coupled it is to the old sheet's DOM, then choose.** (2) **Equip slots + attunement** on the `equipment` column (equip links an inventory item to a body slot; attunement = 3-pip tracker) — this finally fills the empty slots. (3) **Rolls → feed:** wire ROLL/CAST/check on the detail drawer to the shared feed (`feed-bridge.js` already loaded) — makes the sheet *playable*. (4) **Mobile pass** — the diagonal split is desktop-only. (5) **Retire the old `sheet.html` modal** once the new sheet covers editing.

---

**Immediate: verify MOBILE-TRIAGE v2 + UX-AUDIT-2 live.** Deploy `combat.html` (markers: `max-height: 500px`, `UX-AUDIT-2`). Phone portrait AND landscape: open feed, tap composer, type — visible and sendable. Player dblclick enemy → chip. First-load drive hint (player), first bestiary drop hint (staff); clear localStorage to re-test. Get the "next"-bug repro from M while testing.

**Pending M confirmations (asked, unanswered):** (1) M3 auto-load battle.js — did it deploy + smoke? (tracker onto seated monster → staff HUD auto-switches with red toast; marker: `NPC's turn auto-loads` comment in setTurn). (2) Has the turn-skip guard blocked a player in live play yet? (3) Export-archive button pressed once successfully? (4) `disposition` column confirmed live?

**Next major: the MOBILE AUDIT — its own mock-first session.** Direction agreed: a phone is a **companion**, not a small DM screen. Sketch: full-screen panes with a bottom tab bar (Feed / Map / My Character), map as a destination rather than the canvas, HUD condensed to a card, no fog/drawing/calibration tools on phones (DM tooling stays desktop/iPad). New walkthrough persona: "player on a couch with only a phone." Landscape-phone nav/marquee height is this pass's problem. ONE coherent mockup first, M's red pen, then build.

**Also queued: slice 2 leftovers** — the "next" bug (blocked on repro), plus the walkthrough backlog above (pick candidates per round; ADV/DIS visibility outside the feed pane is probably highest-value).

**Then Phase 3B: the replay scrubber** — next major build after mobile, mock-first in a fresh session. Mount point: the chronicle's disabled ▶ REPLAY banner button. Data: `kind:'event'` rows (`combat_start` roster snapshot + `turn`/`hp`/`condition`/`initiative`/`move`/etc. — VERIFY which event types actually exist; `move` IS evented per the Phase-3 notes, but confirm against live data; if moves aren't reliably evented the design scopes to a timeline replay instead of a board replay — settle this FIRST). Design questions for M: board vs timeline vs both; scrub by event vs by round; whether hidden-foe events stay masked for players or replays are staff-only.

**Then cleanup:** retire `netlify/functions/chronicle.js` after a few stable sessions; optionally delete the dead `sheet-prototype.html`; `mockup-server-browser.html` (this session's approved mockup) can be deleted from the repo or kept as a design record — M's call.

**`#quests` page** — M flagged it: pinned-tag channels proved the idea; quests graduate to a dedicated page later.

**Phase 2 cleanup (small, whenever):** verify post-deploy that the Functions tab shows `feed-export-nightly` with the schedule badge and that one manual test-run logs success (requires `SUPABASE_SERVICE_ROLE_KEY` in Netlify env).

**Older pending items:** disposition (friend/foe) feature code (confirm the live `disposition` column first); initiative-chip flag-card redesign (maybe partly done — decide); possible nice-to-have: plain-text editing of chat messages from the combat feed panel (full editing lives on chronicle.html).


## This session (June 17b) — Members & Access (in-site identity/admin) + combat token tray

**Members & Access (BUILT, deploy pending; SQL #1 already RUN on live).** A whole in-site identity + onboarding layer so the overseer never edits the DB by hand to manage who's in and what they can do. **Value-first:** membership attributes (`username`/`role`/`grants`/`character_key`) live on `profiles` now; when a 2nd campaign appears they lift wholesale into a `campaign_members` table (same attributes, new home — the `schema_v1` header already plans this). Site → **campaign-holder** eventually; *Trials of Kirtas* becomes one campaign under a renamed site.

- **`schema_delta_members.sql` (RUN on live; validated 10/10 against a throwaway Postgres copy of the live schema).** Adds `profiles.username` (overseer-assigned, partial-unique on `lower(username)`) + `profiles.grants text[]`; **`'pending'`** added to the role check (a powerless self-provisioned account); **`public.is_member()`** (overseer/dm/player — excludes pending + profile-less guests); **tightened `combatants_player_update` from `using(side='party')` to ALSO require `is_member()`** — closed a latent hole: because OTP login is open, ANY authenticated user (even a profile-less Guest) could write party hp/conditions/x/y/initiative; **`request_access(username)`** SECURITY DEFINER RPC (self-serve — pins the caller to their OWN `pending` row, can't smuggle a role); **`set_membership(uid,role,char,grants,username)`** SECURITY DEFINER RPC (overseer-gated WRITE — character-release before reassign, last-overseer guard, upsert so a never-provisioned login is approved in one click); `profiles` → realtime publication + `replica identity full`; defensive grants; **backfill by email-join to `auth.users` (most-recent sign-in)** that named the 5 accounts AND **FIXED the DM**.
- **DM RESOLVED.** Backfill set hagakuredisc → `dm` on uid `0721407d…` — the email match confirms that IS his active login (so last session's guess was right). M's post-run `profiles` showed the expected 6 rows; the stray 6th — `951d27f3…` (player, null character) — is the old **test account** (Cosmere released, player role kept); M to demote/delete (a click on the new page, or `update profiles set role='pending' where user_id='951d27f3…'`).
- **`members.js` (Netlify, read-only, overseer-gated):** lists `auth.users` ⋈ `profiles` (email, last-seen, invited/confirmed) — the one read the browser can't do (the `auth` schema isn't exposed to `authenticated`). WRITES do not go here.
- **`member-invite.js` (Netlify, overseer-gated):** `invite` → GoTrue `POST /auth/v1/invite` (emails a magic link via SMTP, **bypasses login's `shouldCreateUser:false` lock WITHOUT opening public signup**); `revoke` → admin delete (profile cascades). `redirect_to` = request origin + `/`.
- **`nav.js` whoami:** now selects + exposes `username`, `grants`, and computed `displayName` (`username || email-local || 'Member'`); role union includes `'pending'`. Purely additive — existing `__tok.profile` consumers unaffected.
- **`admin.html` (NEW page, overseer-gated, dark theme via `theme.css`):** the Members & Access page. Invite bar → `member-invite`; **"Waiting at the gate"** with status tags (**Invite pending** / **Arrived** / **Requested**, invite-pending sorted first); per-member username/role/character/grants editing via `sb.rpc('set_membership')`; Resend + **Revoke** via `member-invite`; **`profiles` realtime subscription** so new arrivals appear live. `node --check` + jsdom smoke (overseer gate, queue+table render, own-role lock, role→db map, invite/resend/revoke all hit the endpoint).
- **Decisions locked:** **Invite model, NOT open signup** — login keeps `shouldCreateUser:false`; the overseer admits people. **Grants are additive on top of role** (role stays the RLS backbone); a grant is INERT until a feature checks it — first real one will be combat-token cleanup (touches the combatants write policy). **Pending = inert** (no member role) → opening onboarding is safe.

**Combat page (SHIPPED, deploy pending; one file = `combat.html`).**
- **Duplicate-monster fix.** "Monster can only be added once" was a **UI lock**, not a data limit — `dropMonster` always INSERTs a fresh row; the Drop button just tagged itself `.done` and the handler early-returned. Removed the lock; Drop re-arms; instances **auto-number** (Goblin, Goblin 2, Goblin 3; "Goblin Boss" stays out of the series).
- **Token tray (new "Tokens" dock tab, staff-only).** v1 = **Party shelf**: tap a PC to add/remove its token on the board. Reframe — **board holds instances, tray holds the sources you pull from** → removing a token is safe (re-add from the shelf), closing the "no way to put characters back" gap. Spawns a proper party combatant (`side='party'`/`source_key`/portrait/`hp`=`hpMax` from the legacy `CHARACTERS` map) at board center; remove = delete the combatant. **Hooked into `renderTokens()`** so the shelf's on-board state stays honest even when a token is removed via the right-click menu. **Source is `CHARACTERS` (4 PCs) for now** — becomes the `characters` table (every PC + NPCs) once characters go table-driven. **Saved-monsters shelf is stubbed** ("coming next").

**Deploy checklist (M, one at a time):** `schema_delta_members.sql` (DONE) · `members.js` · `member-invite.js` · `nav.js` · `admin.html` · `combat.html`. Confirm `SUPABASE_SERVICE_ROLE_KEY` in Netlify; **enable the "Invite user" email template** (Auth → Email Templates); **add the site origin to Auth → URL Configuration → Redirect URLs** (else invite links bounce). Live click-through: admin reads the 5, rename/reassign, OTP a throwaway email in → it appears in the gate; combat → open a scene, add/move/remove/re-add a PC, drop Goblin ×3.

**Open / next session:**
- **#1b — `characters` write policy (the one carryover): [RESOLVED June 18 — see that session: A/B/C built + PG16-validated, deploy-ready.]** Was the `using(true)` party-write hole + table-driven unlock. Done as Phase A (`is_member()` tightening) + Phase B (CHECK→FK, table-validated `set_membership`) + Phase C (admin dropdown + combat Party shelf from `loadParty()`). Note: in-app character creation became **Soul Shards** (its own builder), NOT the skeleton-row form once sketched here.
- **Saved-monsters shelf: [RESOLVED June 18 — shipped & deployed, M confirmed working.]** `saved_monsters` table + additive ★ + renamable named variants + tags + spawn-from-shelf; `dropMonster` hoisted to sibling scope with a `nameOverride`. See the June 18 block.
- **Members nav link:** overseer-only "Members" → `admin.html`, using the existing reveal-after-`__tok.ready` pattern (like the character caret).
- **Party-view pagination** (show 4–5, arrow through) on `party.html`; **personal "your characters" view** (rides on `characters.owner`) — future.
- **NPCs-in-`characters` fork (undecided):** do recurring DM NPCs live as `characters` rows (add a `kind`/`is_party` flag) or stay bestiary statblocks? Decide before the FK migration.
- **Repo↔live DRIFT (flag):** several run-live SQL files are NOT committed — `schema_delta_characters`, vitals/hp-sync, `advance_turn`, `grant_service_role`, enemies/disposition deltas. `schema_v1.sql` is NOT the full live schema; commit them so the repo stops lying.

**Lessons pinned this session:**
- **A policy granted `to authenticated` gates on "is logged in," not "is a member."** With open OTP that's anyone → gate party/character WRITES on `is_member()`, not bare `authenticated`.
- **A SECURITY DEFINER RPC called with the SERVICE key has no `auth.uid()`** (service role isn't a user) → `is_overseer()`-style self-gates fail. Call such RPCs from the BROWSER with the user's session token (we do for `set_membership`/`request_access`); the service key is only for the `auth.users` READ in `members.js`.
- **`shouldCreateUser:false` in `login.html` is the closed front door;** admin `invite` (service role) admits people without opening public signup.
- **Provision/seed/backfill by email-join to `auth.users` ordered by `last_sign_in_at desc`** — robust to duplicate logins (picks the active account); the same join fixed the DM with zero access to his machine.

---

## Last session (June 17a) — GEAR net-worth + auth/profiles fixes

**GEAR net-worth + write-back (SHIPPED, deploy pending).**
- **Money model (locked): all math in COPPER, display only whole coins — no decimals/fractions.** Conversions `pp=1000 / gp=100 / ep=50 / sp=10 / cp=1`; **electrum is the oddball** (½ gp, not a clean ×10). `15 gp` *is* `1500 cp` — that's how 5etools stores `value` natively (proxy just `/100` for the "15 gp" string). `coinStr(cp)` decomposes back to gp/sp/cp (e.g. `1.5 gp → "1 gp 5 sp"`, never "1.5"). **Dropped the 50%-sell convention** (full value; table's call). Items with no 5etools price get a manual **set-worth** (entered in gp, stored as copper `manualCp`). Net worth = coins + Σ item value; **to-sell** subtotal = full value of `forSale` items; **unvalued items are surfaced, not hidden** ("N unvalued").
- `netlify/functions/items2.js`: added `valueCp` (raw copper) to `summarise()` alongside the `price` string — safe, backward-compatible.
- `party.html`: `RENDER.gear` (~555) adds per-card value + a `#pm-worth` ledger line; `WIRE.gear` (~618) adds the drawer **SET WORTH** input + **MARK TO SELL** toggle, writing `inventory` via debounced `CharacterData.save(key,{inventory})` with in-place refresh, **gated on `_canEdit`**. New token `--sell: var(--hp-warn)`; new CSS for `.worth*` / `.ci-v` / `.d-set` / `.d-sell`. Verified: `node --check` clean + **15/15** copper-math smoke. Not browser-rendered here (Supabase/nav deps) — eyeball on preview deploy.
- **GEAR architecture facts:** `.citem` cards ARE the keyboard-nav targets; the stored inventory item is **slim** (`{name,detail,qty,weight}`) on add — `value`/`rarity` only appear after the async **enrichment** pass (so the old "inventory[] = full 5etools objs" note is only true post-enrichment). **No characters-table realtime subscription touches `inventory`** → load-on-open + adopt-after-save, no TOAST clobber risk.

**Auth / profiles playbook (hard-won this session).**
- Chronicle staff gate: `IS_STAFF = ME.role==='overseer' || ME.role==='dm'` (~3012). `ME` comes from `window.__tok.ready` (in `nav.js`, ~707–740), which reads `profiles` via `.eq('user_id', <auth uid>)`. **Null profile → "Guest".**
- `profiles_read` RLS is `using (true)` (open); only overseer can WRITE (`profiles_overseer_write`), so users can't self-provision. `profiles.user_id` is `not null unique references auth.users(id)`; **no auth→profile trigger** — seeding is manual and the schema's seed block never even had a DM line.
- **DM was Guest because his login uid had no `profiles` row.** Provisioned `role='dm'` by uid → `0721407d-f8fa-4fb1-8b3e-3bef4f2c60b8` (PENDING M's verify: that the row's email is his and `last_sign_in_at` is recent — i.e. it's the account he actually authenticates as). `character_key` stays **null** for the DM (correct — Narrator plays no PC). **[RESOLVED June 17b: the `schema_delta_members` backfill (email-join, most-recent sign-in) set `dm` on `0721407d…`; the email match confirms it IS his active login. DM fixed.]**
- **Cosmere character rebind:** `character_key='cosmere'` was wrongly on M's **test account**; moved to **ianakira**. Pattern: `update profiles set character_key=null where character_key='cosmere';` then upsert onto the right uid — **respect `profiles_one_per_character` (partial unique on `character_key`): release before reassign.**
- **`openModal is not defined` was NOT the SQL** — it's a JS `ReferenceError` (DB-independent). Confirmed the live `chronicle.html` is current (`function openModal` @1446; deployed markup matches `main`; **no service worker**), and M (overseer) uses chronicle fine → code is good. The DM's error was downstream of being a Guest and/or a stale/pinned build on *his* browser. Fix path: bind `dm` to his real uid + reload the canonical `trials-of-kirtas.netlify.app/chronicle.html`.

---

## Earlier history (background)

- **Built across prior sessions:** the full site from scratch — shared nav (`nav.js`), `theme.css` var system ("Phantom" theme: cream/off-white + heavy black ink + red accent; Cinzel / Barlow Condensed / EB Garamond), NPC/location tooltips, character sheet engine (`sheet.html`) with dice roller + HP tracker, compendium (5etools GitHub mirror), drag-and-drop inventory with nested containers + pack auto-explode (`items2.js` Netlify fn), world map (`world.html`) pan/zoom with pins, the battle HUD (`battle.js`), the NPC page (`npcs.html`, flag-card style), the chronicle page, the site-wide identity/auth layer (Supabase OTP, `nav.js` whoami, route protection, seat-defaulting on `sheet.html`).
- **Shared feed Phase 1 (prior session):** the "Game feed" hub on combat.html — Combat/Chronicle/All tabs, /roll parser (NdM, kh/kl, flat mods), Discord-style rows with PORTRAITS/TOKENS avatars + side-colored rings, quick-dice, staff hidden toggle, HUD rolls via the logRoll seam, DM "Roll all" posts NPC init summary hidden. Same session: in_combat flag + initRoster/turnOrder, combat lifecycle right-side controls (Display / View as player / Combat dropdown), stateful HUD init roll honoring Adv/Dis.
- **Combat page features (prior):** token movement w/ Chebyshev overlay, fog-of-war (feathered SVG mask, reveal/hide brush, client-side tier OK for trusted group), per-token conditions (8 badges, context menu), token size (Tiny–Gargantuan), shared drawing layer (AoE templates), live cursors (Realtime Broadcast/Presence; staff cursors hidden from players), tabbed role-aware context menu, bestiary/monster system (DM picker from the 5etools CDN mirror, `ac` + `statblock jsonb`, statblock slide panel lifted from `compendium.html`, monster token art w/ letter-disc fallback), HP bridge between HUD and `combatants` (verified end-to-end).
- **Bardic Console:** React/JSX, Web Audio API, Cloudinary-hosted audio (migrated from Dropbox), mood pads, fader link groups, scene saving, Sonus Portals (YouTube IFrame API).
- **Other:** "The Apothecary's Craft" potion-crafting reframed as a ledger/reference. Began planning modernization of a large Indian classical music archive (currently FileMaker Pro) → Python/PostgreSQL local web server, raga-centric hierarchy, cross-raga linking, multi-user.

---

---

## Last session (June 22b) — sheet play-surface: every roll connected (slots · dice-engine · attacks · feed · cantrip bridge · checks)

Goal of the session: make the **v11 at-a-glance sheet** (`sheet-v2.html` / `sheet-mount.js`, mounted live in the combat float) **fully rollable** — "every single aspect of the sheet connected." Built up the play surface in layers, all validated by jsdom smokes; deploy was incremental and **fell out of sync near the end** (see OPEN #2).

**SHIPPED this session (staged):**
- **`dice-engine.js` (NEW) — the one shared roll engine.** Pure + stateless; takes modifiers as args (`{advantage,disadvantage,bless}`). `rollAction(action,opts)` (attacks: to-hit + damage, nat-20 doubles dice) and **`rollCheck(label,mod,opts)` (NEW)** (flat d20 + mod for ability/save/skill/initiative — same d20 roller + bless, no damage, nat 20/1 flagged structurally only). Formatted `main`/`detail`/`dmg` are **byte-identical to battle.js** (HUD parity in the feed). Loads as `window.DiceEngine` + CommonJS. Golden smoke proves `rollAction` == battle.js.
- **Spell slots + casting + concentration** (`buildSpellcasting` in sheet-mount.js + sheet-actions.js). Slots = `vitals.pipState` SPENT keyed by pool (`pactSlots`/`spell_<L>`/`sorc_<L>`/`sorcery`), max from `classFeatures` → sheet + orbs lockstep, rests refill. **Player ALWAYS picks level + pool** (`openCastPicker`); concentration banner with replace-guard at commit. Manual pip nudges are QUIET; only casts + concentration changes hit the feed.
- **Attacks roll surface** (`renderActions` in sheet-mount.js + sheet-actions.js). An **Actions panel before Spellcasting** — grouped **Attacks / Bonus damage / Utility**, an Adv/Dis/Bless toggle bar (consume per roll), tap-to-roll via DiceEngine, v11-native result cards from the engine's STRUCTURED fields, utility rows inert. `deriveActionMods(a,s)` = hit/dmg from chosen `ability` + proficiency, **raw `hitMod`/`dmgMod` fallback** so existing flat data still rolls.
- **FEED-ROUTING FIX (was the "rolls don't reach the feed" bug).** The sheet had been posting via `window.__battle.onLogRoll` (the feed-bridge hook) — but **battle.js's HUD ALWAYS takes `backend.logRoll`** (always a truthy fn), so `onLogRoll` is a HUD *fallback that's never exercised*; it was effectively dead for the sheet. FIX: the sheet now writes **directly to `feed` via `window.__tok.sb`** (`postFeed` in sheet-actions.js) — the same client it loads characters with, matching `combat.html`'s proven `feedInsert` exactly (`{channel:'combat',kind:'roll',actor_key,actor_name,body,hidden:false,session,encounter_id}`; `actor_name = structural.name`; session + active encounter resolved & cached 30s; visible `[sheet-feed]` error logging). Removed the now-unused `feed-bridge.js` tag from `sheet-v2.html`.
- **Cantrip bridge.** Tapping a **damage cantrip in the Spellcasting list** (Booming Blade, Eldritch Blast, Green-Flame Blade) rolls its **matching action** (matched by label; **only rollable types** — a utility match like Minor Illusion falls through to "cast · cantrip"). `doCast` cantrip branch → `actionByLabel` → `doRoll`. **Cantrips only** this session; leveled damage spells still slot + announce.
- **Checks — abilities / saves / skills / initiative all roll.** Every `[data-chk]` row rolls d20 + its shown modifier via `DiceEngine.rollCheck`, sharing the same Adv/Dis/Bless toggles + result area + feed. `renderAbilities`/`renderSaves`/`renderSkills` stamp `data-chk`/`data-chk-label`/`data-chk-mod` + make rows clickable; the **initiative tile** is clickable too. Labels: "Strength", "Dexterity Save", "Acrobatics", "Initiative". Verified end-to-end: `Charisma: [15] +3 = 18`, `Persuasion: [11] +5 = 16`, `Dexterity Save: [8] +2 = 10`, `Initiative: [17] +2 = 19`.
- **`combat.html` engine fix (CRITICAL — was "nothing works on the float").** The floating sheet runs on `combat.html`, which loaded `sheet-mount.js` + `combat-sheet-float.js` + `sheet-mount.css` but **NOT `dice-engine.js`** → `window.DiceEngine` undefined → every engine-backed roll (checks, attacks, bridge) silently hit the "engine offline" guard while **casts worked** (they `postFeed`, no engine). FIX: added `<script src="dice-engine.js"></script>` before `sheet-mount.js` on `combat.html`. **RULE: any page that mounts the sheet (`sheet-mount.js`) MUST also load `dice-engine.js`** — currently `combat.html` + `sheet-v2.html`; if the site-wide right rail later mounts the sheet elsewhere, it needs it too.

**OPEN — next session, fresh eyes (M's priority):**
1. **Render the float at the FULL v2 layout (not a swap).** KEY FINDING for the fresh session: `combat-sheet-float.js` **already mounts the real v2 sheet** — `window.mountSheet(page,key)` at line ~134, the *same* `sheet-mount.js` as `sheet-v2.html`. There is **no separate/slimmer sheet**. What M reads as "a different format, attacks further down" is that **same sheet reflowing in the narrower float window** (columns collapse, Actions drops below the fold). M's call: the float is **not so narrow that the full sheet won't fit** — make it render at the full two-column v2 layout. So the work is a **width/CSS fix** (float window default size in `combat-sheet-float.js`/`.css`, and/or the sheet's responsive breakpoints in `sheet-mount.css`), **not a sheet replacement**. M: "approach slightly differently perhaps."
2. **Skills + cantrip-bridge silent on the float ≈ deploy version skew.** Attacks (`rollAction`) are the OLDEST code and roll; the **bridge + checks (`rollCheck` + the `data-chk` rows + `doCheck`) are the NEWEST edits** spread across `sheet-actions.js` + `sheet-mount.js` + `dice-engine.js`. If those three aren't ALL at latest live, attacks roll but checks/bridge silently no-op — exactly M's symptom. **Resolution: deploy ALL of {`combat.html`, `dice-engine.js`, `sheet-mount.js`, `sheet-actions.js`, `sheet-mount.css`} at latest, together + HARD-REFRESH `combat.html`.** Smokes prove the logic (engine 47, attacks 47 incl. checks+bridge, slots 37). If still broken after a clean full deploy + hard refresh, only THEN investigate the float context specifically.

**STILL PENDING (carried, unchanged):**
- **Attack EDITOR** (mock APPROVED: `/mnt/user-data/outputs/mock-attacks.html`): edit/customize attacks; pick driving ability (Hexblade→CHA, hit/dmg recompute from abilities+prof); toggle "add ability mod to damage" (the Eldritch Blast +CHA Agonizing-Blast fix); edit damage type; add/remove; crit auto-doubles. `deriveActionMods` **already** honors the structured override (`ability`/`proficient`/`atkBonus`/`dmgAbility`/`dmgBonus` over raw `hitMod`/`dmgMod`). Build: editor modal in sheet-mount.js + edit-mode + save-via-optimistic-path in sheet-actions.js + sheet-mount.css.
- **Leveled-spell damage rolling** (bridge covers cantrips only; upcast scaling is its own piece).
- **HUD-onto-engine dedupe** (battle.js's 6 dice fns → thin delegations to `DiceEngine` + add `dice-engine.js` to the 9 battle.js pages). Pure refactor; golden smoke proves no-op. M cleared it; own preview check.

**Validation harness (all green this session):** dice-engine **47**, sheet-mount 26, sheet-actions 24, sheet-resources 15, sheet-trackers 32, sheet-slots **37**, sheet-attacks **47**, orbs-custom 14, shards-forge 18. (`smoke-sheet-data` fails pre-existing/unrelated — ignore.) **jsdom gotchas:** scripts eval'd into the jsdom window use `window.Math.random` (a DIFFERENT realm than node's global `Math.random`) — stub BOTH; the sheet's feed posts go through `window.__tok.sb`, so smokes stub that with a mock that captures the inserted row.

## This session (June 23) — the float at **full v2 width** (container-query reflow), **per-character appearance INSIDE the float**, the **in-sheet roll feed removed from the float**, and the concentration **`[object Object]`** fix

Continued the float thread from June 22b: render the v11 sheet at its full two-column layout in the floating window (June 22b **OPEN #1**), borrow the per-player appearance system into the float, and clear two display bugs. All four pieces shipped + validated; M deployed each and confirmed live.

**SHIPPED this session (staged):**
- **Float renders the full two-column v2 sheet (June 22b OPEN #1 — RESOLVED).** Diagnosis: the float **already mounts the real sheet** (`mountSheet`); it only *looked* narrow because `sheet-mount.css` un-collapses the sheet's two-column `.body-grid` (`312px 1fr`) at an **880px VIEWPORT `@media`** — which never fires for a window sitting on a desktop viewport — so the float was stuck single column, and `combat-sheet-float.css` had forced single column at its 480px default. FIX: made the sheet's scroll container a **CSS `@container`** (`container-type:inline-size`) so the reflow keys off the **window's OWN width**, not the viewport — clean single column when the window is narrow, full **two-column v2 at ≥878px** (matches the sheet's native 880px). Default window widened **480 → 900px**, height `min(80vh,820px)`. Geometry `localStorage` key bumped **`tok.sheetFloat.v1` → `v2`** so a stale saved 480px width resets. Popovers verified safe (cast picker / HP stepper are `position:fixed`, appended to `document.body`, OUTSIDE the container). **M deployed — "this is working."**
- **Per-character appearance painted INSIDE each float tab (NEW).** The standalone sheet's appearance system (`appearance.js` + `appearance-data.js`; full-screen `position:fixed` layers behind the page) now also paints **per tab inside the float**. Design (M's call): **per-character, READ-ONLY** — writes still go through the owner-only `set_my_appearance` RPC via the cog on the standalone sheet; the float only **READS** each character's owner's saved look **by `profiles.character_key`** (the existing `profiles_read` policy already lets any authenticated user select any profile → no new policy, no per-character storage). Tab restructured into a **non-scrolling `.sf-bg`** (the wallpaper) behind a **scrolling `.sf-scroll`** (the sheet mounts here; `.sf-scroll` now carries the `container-type` for the reflow) — so the look stays put like a desktop wallpaper while the sheet scrolls over it. (`renderSheet` is purely `querySelector`-based and never resets the container's `innerHTML`, so the injected `.sf-bg` survives every realtime refresh.) NEW module **`appearance-float.js`**: reuses the catalog (`appearance-data.js`) + the **exported `DEFS`/`WEAVE`/`GRAIN`** from `appearance.js` (made `export` — additive, standalone sheet untouched), injects the `.tok-*` layer stack into `.sf-bg`, reads `profiles.appearance` by `character_key`, paints a default first (no flash), swallows any error (cosmetic only). **`--ac-hue` is set directly ON the `.sheet` element** — its own local `--ac-hue:0` from sheet-mount.css would otherwise win. Loaded via dynamic `import('./appearance-float.js')` from `combat-sheet-float.js` (no `combat.html` change). Supabase client is `window.__tok.sb`.
- **The sheet's in-sheet roll feed REMOVED from the float.** The v11 sheet has its own roll-result feed (`[data-list="actionResult"]`, rendered by `renderActionResult` in sheet-mount.js, capped at the last 4 cards) — meant for the **standalone** sheet, which has no right rail. In the **float** it DUPLICATED the right-rail feed AND grew the Actions block, pushing Spellcasting down. First tried a float-scoped CSS hide (`.sheet-float [data-list="actionResult"]{display:none}`) — correct on paper (top-level, specificity (0,2,0), loads after sheet-mount.css) but it **did not take effect live** (stylesheet/cache skew). FINAL FIX: **remove the element in JS** in `combat-sheet-float.js` — `mountSheet(scroll,key)` stamps the template **synchronously**, so right after it, `scroll.querySelector('[data-list="actionResult"]').remove()`. `renderActionResult` then **no-ops on every roll** (`var host=root.querySelector(...); if(!host) return;`), while the right-rail feed (`feedPostRoll`/`postFeed`, a separate path) keeps logging. Deterministic — can't depend on the cascade. **Standalone `sheet-v2.html` keeps its in-place feed** (its only roll feedback). **M: "That fixed it."** LESSON: when a CSS hide *should* work but doesn't land live, removing the node in JS at mount is the bulletproof move (and correct when the element is genuinely unwanted in that context).
- **Concentration `[object Object]` fix.** `setStatus` in sheet-mount.js rendered the STATUS row with `conc.textContent = v.concentration` — but `vitals.concentration` is the **`{name,duration}` object** (set that way on cast, and the `data-conc-banner` correctly reads it as an object). FIX: `conc.textContent = v.concentration.name || v.concentration` (legacy-string fallback). Shared file → fixes **both** the standalone sheet and the float.

**RESOLVED from June 22b OPEN:** **#1** (float full v2 layout) — done via the container query. **#2** (skills + cantrip-bridge silent on the float = deploy version skew) — a clean full deploy + hard-refresh sorted it; everything rolls on the float now.

**STILL PENDING (carried, unchanged):**
- **Attack EDITOR** (mock approved `mock-attacks.html`): ability picker, per-attack damage-type edit, the "add ability mod to damage" toggle (the Eldritch Blast +CHA Agonizing-Blast fix), add/remove, crit auto-doubles. `deriveActionMods` already honors the structured override (`ability`/`proficient`/`atkBonus`/`dmgAbility`/`dmgBonus` over raw `hitMod`/`dmgMod`).
- **Leveled-spell damage rolling** (the bridge covers cantrips only; upcast scaling is its own piece).
- **HUD→engine dedupe** (battle.js's 6 dice fns → thin `DiceEngine` delegations + add `dice-engine.js` to the 9 battle.js pages; golden smoke proves no-op).
- **Live combat caveats (from June 22, still open):** conditions + initiative are `combatants` fields (NOT `vitals`) so they don't ride the `characters` realtime channel — the **combatants-realtime handler must re-bloom the orbs** for those; the four PCs' `characters.structural` is a wrong **aggregate** (rebuild from Roll20 — root cause of both the wrong sheet AND thin orbs); the ribbon **"+"** party-picker is inert (`onAdd`/`combatsheets:add`, nothing listening); the right rail is combat-only / old.

**Validation harness (green):** **sheet-mount 26/26** (against the concentration fix), dice-engine 47, sheet-attacks 47, sheet-actions 24, sheet-slots 37, sheet-resources 15, sheet-trackers 32, orbs-custom 14, shards-forge 18. All touched JS `node --check` clean — `sheet-mount.js`, `combat-sheet-float.js`, `appearance.js`, `appearance-float.js`. (`smoke-sheet-data` still fails pre-existing/unrelated — an ENOENT for a mockup file absent from a fresh clone; ignore.)

**Files this session (all staged in `/mnt/user-data/outputs/`, M deploys):**
- **`combat-sheet-float.js`** — window 480→900 default geometry (LS `v1`→`v2`); `.sf-bg` + `.sf-scroll` tab restructure (sheet mounts into `.sf-scroll`); **JS removal of the in-sheet roll feed**; dynamic-import + paint of the per-character appearance; `refresh` renders into `.sf-scroll`.
- **`combat-sheet-float.css`** — `@container` reflow moved onto `.sf-scroll` (≥878px → two-column); width 900 / height `min(80vh,820px)`; `.sf-bg` + the `.tok-*` layer styling; the (now-superseded) actionResult CSS hide retained as a harmless fallback.
- **`appearance-float.js`** (NEW) — the float-scoped appearance painter/loader (`applyFloatAppearance` / `loadFloatAppearance` by `character_key`).
- **`appearance.js`** — `export` added to `DEFS` / `WEAVE` / `GRAIN` (additive; standalone sheet untouched).
- **`sheet-mount.js`** — the concentration `.name` fix.
- NOTE: this session's `combat-sheet-float.css` is restructured (`.sf-bg`/`.sf-scroll`) — **deploy it together with `combat-sheet-float.js`**; the appearance layers need both.

*Last updated: end of the **June 23** session — the floating v11 sheet now renders at its **full two-column width** (container-query reflow off the window's own size, not the viewport), paints **each character's saved appearance** as a per-tab wallpaper (read-only, by `character_key`, reusing the existing appearance engine), **no longer duplicates the roll feed inside the sheet** (the in-sheet `[data-list="actionResult"]` element is removed in JS at mount so `renderActionResult` no-ops; the right-rail feed is untouched), and the concentration STATUS row shows the spell name instead of `[object Object]`. Staged this session, NONE pushed by C (M deploys): `combat-sheet-float.js`, `combat-sheet-float.css`, `appearance-float.js` (NEW), `appearance.js`, `sheet-mount.js`. **June 22b OPEN #1 + #2 resolved.** NEXT: the **attack editor** (approved mock), **leveled-spell damage**, and the **HUD→engine dedupe** — plus the live June-22 caveats (combatants-realtime re-bloom for conditions/initiative, rebuild the PCs' real `structural` from Roll20, the inert ribbon "+", the combat-only right rail). Working rules unchanged: read repo source before editing; `node --check` + jsdom smokes before handover; mock→approve→build; never change a theme var for a per-page issue; **C never pushes — M commits/deploys manually.***

## This session (June 24) — Soul Shards **racial-spell capture** (Yuan-Ti spells land), two **spell-render fixes** (cantrip mislabel + `[object Object]` casting time), **non-caster sheet hygiene**, and the **Appearance customizer moved into the rail's Settings tab** — which surfaced **three latent bugs** in the appearance stack (a silently-failing float painter, a page-paint side-effect, and a dead Settings tab). All shipped + validated; M confirmed the customizer is live. **ONE issue carried to next session: the mounted sheet is being reshaped by the host page's CSS.**

Continued from June 23. Two threads: finish the Soul Shards spell pipeline for **non-casters that get spells from their race**, and make the per-character appearance system **editable from the rail** (June 23 only painted the float read-only — you could see a look but not set one without the standalone-sheet cog).

**SHIPPED this session (staged):**

- **Racial-spell capture for non-casters (Soul Shards "Gap 1" — RESOLVED).** A non-caster class (e.g. Rogue) with a spell-granting race (Yuan-Ti) now produces a real `spellcasting` block on the sheet — racial ability + DC, spells bucketed by level, tagged by origin. The Yuan-Ti race's `additionalSpells` (Poison Spray cantrip · Animal Friendship · Suggestion) are all captured. Three files: **`shards.html`** — `SpellsUI.racialAbility()` resolves the racial casting ability (fixed, or highest-of when the race offers a `choose`), `spellsForStructural` stamps a per-spell `ability`, and the forge's `picked`/`spellsReady` gates were **un-gated from `isCaster`** (a non-caster with racial spells still needs them collected); **`soul-shards-spellcasting.js`** — `castMeta` falls back to the **racial ability/DC** when there are no class casters, and `bucketSpells` preserves each spell's `ability`; **`soul-shards-derive.js`** — `anyCaster` is now also true when `input.spells` has any `origin==='race'`. Validated by **`smoke-spells-racial.mjs` (18/18)**. **M re-forged the Rogue → the three Yuan-Ti spells appear, tagged YUAN-TI.**

- **Two spell-render bugs fixed (`sheet-mount.js` + `soul-shards-spellcasting.js`).** (1) **Every spell logged as "cast · cantrip"** — the cast handler reads `data-level`, but `bucketSpells` never put a `level` on each spell so `spellHTML` wrote `data-level="0"`. FIX: `bucketSpells` now adds a per-spell + per-group `level`, and `spellHTML`/`groupHTML` resolve level with a **heading-parse fallback**, so **already-forged characters read correctly without a re-forge** (Suggestion→2, Animal Friendship→1). (2) **`[object Object]` casting time** — `timeOf` returns the raw 5etools time array; added **`fmtCt()`** in sheet-mount.js to format string/array → "1 action".

- **Non-caster sheet hygiene (`sheet-mount.js`).** `renderSpellcasting` now computes whether the character actually casts (`saveDC != null || groups || pools`); if not, it hides `.cast-meta` + the legend, dashes castAbility/DC/attack/type, and shows **"This character doesn't cast spells."** Each field also dashes individually when its own value is missing — so the SHEET_TEMPLATE's baked-in **Cosmere sample** (DC 13 / +5 / Charisma / "Cosmere Runestar" footer) no longer bleeds through when a character's `structural` lacks a `spellcasting` block (that sample is the template default that `setF` leaves untouched on null — it was *not* a real revert, a recurring false-alarm this sprint). Validated by **`smoke-sheet-noncaster.mjs` (10/10)**.

- **Appearance customizer in the rail's Settings tab (NEW) — the headline.** June 23 painted the float read-only; now **rail → Settings** carries the full `buildAppearancePanel` studio (background picker, geometry, hue/sat/grain/weave/scan/blur/vignette/glitch), and its **live preview drives the floating sheet** in real time. NEW module **`appearance-settings.js`** (loaded non-blocking by `rail.js` as an ES module): on `tok-rail:ready` it fills the built-in Settings pane with `buildAppearancePanel`, passing **`onApply: paintFloats`** so every control change repaints the open float(s) — `document.querySelectorAll('.sf-page').forEach(p => applyFloatAppearance(p, cfg))` — instead of the page. `appearance.js`'s `buildAppearancePanel` gained an **`onApply` hook** (`const apply = opts.onApply || applyAppearance`; all four live-preview calls routed through `apply`) so the standalone sheet still paints the page while the rail copy paints the mount. Save is the panel's own button → `set_my_appearance` (owner-only). **M: "the settings works now."**

  Getting it working surfaced **three latent bugs** — all fixed:
  1. **The float's wallpaper painter was silently failing.** `appearance-float.js` imports `{DEFS,WEAVE,GRAIN}` from `appearance.js`, but in the working code those three were defined as `const` and **not exported** (June 23 *staged* this export, but it wasn't in the live/repo code this session, so the float painter was still dead). The import threw, `combat-sheet-float.js` swallowed it (`.catch(()=>{})`), and **a mounted sheet painted no background at all** — the real reason floats looked flat (not "no saved look"). FIX: (re-)added **`export { DEFS, WEAVE, GRAIN };`** in `appearance.js`. **Deploy `appearance.js` even apart from the customizer — it's what makes the float paint at all.**
  2. **The customizer painted the PAGE, not the mount.** `appearance-settings.js` first called `loadAppearance(sb,uid)` to seed the panel — but **`loadAppearance` calls `applyAppearance` as a side-effect** (appearance.js ~111/116), and `applyAppearance → ensureLayers()` stamps the page-level **`#tok-bg`/#fx** layers onto whatever page it runs (so the **shards forger page** got wallpaper+grain+glitch while the float stayed flat). FIX: the rail module now reads the saved look with a **raw `profiles.select('appearance').eq('user_id',uid)` query — no `applyAppearance`**. (loadAppearance's paint is *correct* for `appearance-boot.js` on the standalone sheet, where the page IS the sheet — so the fix is in the rail module, not loadAppearance.)
  3. **The Settings tab did nothing — it was a dead stub.** The rail's built-in Settings button is hardcoded **`class="tr-tab future"` with no `data-rail-tab`**, and the tab click handler (`rail.js` ~392) binds only `.tr-tab[data-rail-tab]` and explicitly skips `.future`. So the pane the customizer mounts into was **unreachable**. FIX: made that button a real tab (**`class="tr-tab" data-rail-tab="settings"`**); the existing handler then wires it automatically → `setTab('settings')` switches to the pane. Codex stays `future` (no feature behind it yet).

**OPEN — carried to the NEXT session (the one thing left on the sheet):** → **RESOLVED June 25 (see the June 25 entry below).**
- **The mounted sheet is being reshaped by the host page's CSS ("forced formatting").** With the Rogue float open on **`shards.html`**, the sheet renders wrong: a **dark orb-grid band above the header**, **gray blurred bands behind the section titles** (Abilities / Skills / Spellcasting), the parchment squeezed. The mounted sheet should render **self-contained**, exactly as the standalone `sheet-v2.html` does. Root cause is **CSS isolation** — the float / mounted sheet is inheriting or colliding with `shards.html`'s self-contained Phantom palette and/or the float chrome, instead of being sealed off. Next session: **isolate the mounted sheet from host-page CSS** — a hard scope/reset boundary around `.sf-scroll` / `.tok-sheet`, or move the mount into a shadow root. Diagnose against `shards.html` (most visible there) and confirm `combat.html` behaves the same. NB: the appearance now paints correctly *inside* the float, so this is purely a layout/typography bleed, separate from appearance.

**STILL PENDING (carried, unchanged):**
- **Per-spell spellcasting-ability EDITOR** (mocks approved: `spell-editor-mock.html` per-source + `spell-editor-mock-v2.html` per-spell ability chip + per-class defaults bar, by-level grouping). The per-spell `ability` already rides in every spell object after this session's capture work, so the data is ready for the UI.
- **Soul Shards "Gap 2" — feat-granted spells** (Magic Initiate / Fey Touched / Shadow Touched `additionalSpells` + the Magic Initiate sub-picker).
- **Notes-tab Quill editor** (the sheet's Notes tab is still a stub).
- **Attack EDITOR** (`mock-attacks.html` approved): ability picker, per-attack damage-type, "add ability mod to damage" toggle (Agonizing Blast), add/remove, crit auto-double. `deriveActionMods` already honors the structured override.
- **Leveled-spell damage rolling** (cantrip bridge only today; upcast scaling its own piece).
- **HUD→engine dedupe** (battle.js's 6 dice fns → `DiceEngine` delegations across the 9 pages).
- **Live combat caveats:** conditions + initiative are `combatants` fields (not `vitals`) so the combatants-realtime handler must re-bloom the orbs; the four PCs' `characters.structural` is a wrong aggregate (rebuild from Roll20); the ribbon "+" party-picker is inert.

**Validation harness (green):** new — **spells-racial 18/18**, **sheet-noncaster 10/10**, **appearance-settings 10/10**, **rail-settings-tab 6/6**; updated — **rail 47/47** (Settings is now a live tab, so the future-tab count is 1 not 2). Regression: sheet-mount, dice-engine 47, sheet-attacks 47, sheet-actions 24, sheet-slots 37, sheet-resources 15, sheet-trackers 32, orbs-custom 14, shards-forge 18 — all green. Full suite **27/28** — the lone failure is the pre-existing `smoke-sheet-data` ENOENT (a mockup file absent from a fresh clone; ignore). All touched JS `node --check` clean.

**Files this session (all staged in `/mnt/user-data/outputs/`, M deploys):**
- **`shards.html`** — racial casting ability (`racialAbility`), per-spell `ability` in `spellsForStructural`, forge gates un-gated from `isCaster`.
- **`soul-shards-spellcasting.js`** — `castMeta` racial ability/DC fallback; `bucketSpells` per-spell `level` + `ability`.
- **`soul-shards-derive.js`** — `anyCaster` includes race-origin spells.
- **`sheet-mount.js`** — non-caster hygiene in `renderSpellcasting`; per-spell `level` + heading-parse fallback; `fmtCt()` casting-time formatter.
- **`appearance.js`** — re-added `export { DEFS, WEAVE, GRAIN }` (THE float-painter fix); `onApply` hook on `buildAppearancePanel`.
- **`appearance-settings.js`** (NEW) — the rail Settings customizer; raw `profiles` read (no page paint); `paintFloats` routes the preview to the open float(s).
- **`rail.js`** — bootstraps `appearance-settings.js` (module); **Settings tab made real** (`data-rail-tab="settings"`, dropped `future`).
- Smokes: NEW `smoke-spells-racial.mjs`, `smoke-sheet-noncaster.mjs`, `smoke-appearance-settings.mjs`, `smoke-rail-settings-tab.mjs`; updated `smoke-rail.mjs`.
- **Deploy notes:** `appearance.js` is the critical one (the export makes the float paint at all — deploy even without the customizer). The customizer needs `appearance.js` + `appearance-settings.js` + `rail.js` together; `appearance-settings.js` self-injects `appearance.css`, so **no page-HTML change is required**. `appearance-float.js` / `appearance-data.js` unchanged.

## This session (June 25) — mounted sheet **sealed against host-page CSS**: the v11 sheet is now a fully self-contained, drop-anywhere component (June 24's lone OPEN — RESOLVED)

Continued from June 24. One thread: the floating v11 sheet rendered wrong mounted over `shards.html` ("forced formatting" — a light parchment card with dark type, the appearance wallpaper bleeding through the gaps). Diagnosed to root cause and fixed **structurally** (made the sheet immune) rather than patched at the boundary.

**Root cause — NOT a "forced theme."** The v11 sheet is transparent *by design*: `sheet-mount.css` sets no background on its root; the standalone `sheet-v2.html` gets its dark Metaphor look from the *page's* `body` ground showing through behind the transparent sheet. Mounted into a host that shares the one global light-DOM CSS namespace, two host vectors reached in — (1) **class collision**: the sheet root wore a bare `.sheet` class, and `shards.html` styles its OWN Review-step card with `.sheet{ background:var(--bg-card)… }` (`--bg-card:#fbf8f1`, opaque) — that bare rule landed on the v11 `<main class="tok-sheet sheet">` and, since `sheet-mount.css` sets no root background, **won uncontested** and repainted the dark sheet as a light card (the dark/gray "bands" were the wallpaper showing wherever that card bg didn't reach); (2) **inheritance**: `shards.html`'s `body{ color:#18130d }` inherited into the sheet because the root set no base color/font. *Key correction to M's hunch:* `shards.html` deliberately does NOT link `theme.css` (its own comment says so), so this was shards' own inline styles, not a forced theme injection. `nav.js` does stamp `data-theme` on `<html>` everywhere and `theme.css` (loaded on most *other* pages) carries a global `*` reset + body styling + a noise overlay — so on those pages the sheet has to survive that global regime instead; same class of problem, different host.

**Decision (locked with M): the sheet is a FIXED visual identity.** It keeps its own hardcoded tokens and always reads as "the sheet" no matter where the site theme goes — it does NOT follow a future player-selectable site theme; it owns its own look so it pops out as a distinct surface. (When the site-wide theme picker is built later, decide *per surface* which ones read shared tokens; the sheet stays fixed.) **Principle adopted for all new work going forward:** theme via **tokens / CSS-vars only, never global rules**, and **every shared component namespaces under its own root + sets its own base** — so theming becomes "swap values," collisions become structurally impossible, and the blunt global `*`/body stuff is unnecessary. The v11 sheet is the first component brought fully under that principle; the rail and float are the next candidates to converge opportunistically.

**SHIPPED this session (staged):**
- **Dropped the collision-prone bare `.sheet` class from the sheet root.** `SHEET_TEMPLATE` is now `<main class="tok-sheet">` (was `tok-sheet sheet`). The sheet no longer claims a generic name any host could match, so `shards.html`'s `.sheet{}` — or *any* host's `.sheet`/`.card`/etc. — can never reach its root again. Retargeted the only two consumers of the bare class: `combat-sheet-float.css`'s float-skin **root** rules (`.sheet-float .sheet` → `.sheet-float .tok-sheet`, ×2 — preserves the window reflow; the float's many `.sheet-float .<descendant>` rules were untouched, they match regardless of the root class) and `appearance-float.js`'s `--ac-hue` setter (`querySelector('.sheet')` → `'.tok-sheet'`).
- **Gave `.tok-sheet` its own base text properties** — `color:var(--cream); font-family:"EB Garamond",Georgia,serif; line-height:1.5` on the root rule — so nothing inherits from any host `body`. These exactly match what `sheet-v2.html`'s `body` already set, so the standalone is visually identical; combined with the class drop, the sheet now renders the same on `shards.html`, `combat.html`, and standalone.
- **Restored the float's per-tab appearance wallpaper (a SECOND drift, found after deploying the above).** With #1 deployed the sheet rendered correctly dark, but the appearance background only painted a band at the top and the sheet sat below it ("background stops 1/3 down; images not showing"). Cause: the committed `combat-sheet-float.css` was **missing the entire `.sf-bg` / `.sf-scroll` layering + the `.tok-*` layer stack** — `appearance-float.js`'s own header says the layers are "positioned by combat-sheet-float.css," but those rules (staged June 23) were never committed, so the injected layers fell into normal flow as a top band. FIX: made `.sf-page` the pinned stage (`overflow:hidden`), added `.sf-bg` (absolute, full-bleed, behind) + `.sf-scroll` (absolute, the scroller, over it), and the seven `.sf-bg .tok-*` layer rules — mirroring `appearance.js`'s canonical page-level `#tok-*` stack (same `--bg-*`/`--fx-*` vars, same textures, the geometry patterns incl. `geo-crosses`) but positioned **absolute** within `.sf-bg` instead of `fixed` to the viewport. Now the wallpaper fills the window and stays put while the sheet scrolls over it (desktop-wallpaper model). Demoed in `wallpaper-demo.html`.
- **Considered + rejected Shadow DOM for now** (logged so it isn't re-litigated): true isolation, but it'd break `document.getElementById` (`#hd-med`, `#insp-toggle`), orphan the body-appended cast/HP popovers from the sheet's CSS, and break the `url(#filter)` SVG refs. The namespace + own-base approach is the right cost/robustness point. Shadow DOM stays the "bulletproof against a genuinely hostile host" upgrade if the sheet is ever mounted somewhere that warrants it.

**Validation harness:** **sheet-mount 26/26** (the two assertions that looked up the root by the old `.sheet` class were retargeted to `.tok-sheet` — intent unchanged: "sheet stamped into the container"), sheet-actions 24, sheet-resources 15, sheet-trackers 32, sheet-attacks 47, sheet-noncaster 10, sheet-gear-bio 18, orbs-custom 14, shards-forge 18 — all green. All touched JS `node --check` clean (`sheet-mount.js`, `appearance-float.js`; `combat-sheet-float.js` untouched, re-checked). **Two smoke failures that pre-dated this change were diagnosed and FIXED (stale tests, NOT shipped bugs).** `smoke-sheet-slots` ("concentration banner renders Hex") asserted on a `[data-conc-banner]` element that exists ONLY in that smoke — the shipped sheet renders concentration via the `.conc-strip` `[data-conc-val]` row (`renderConcentration` injects the `[data-conc-drop]` button there). `smoke-rail` (44/46) still asserted **2** `future` tabs ("Codex + Settings disabled") when the committed `rail.js` already makes **Settings a LIVE tab** (`data-rail-tab="settings"`, only Codex is `future`) — matching what's deployed (the appearance customizer runs in Settings; M's screenshot shows it live). So the committed *implementation* was current and correct; the committed *smokes* had simply lagged behind it (the "Add files via upload" flow shipped newer impl with older test files). Retargeted both smokes to the shipped DOM — **no production code touched**. (Also surfaced: `smoke-rail-settings-tab.mjs`, which the June-24 file list claims, was never committed — a *missing* test, not a failure; restore it from the June-24 session if wanted.) Full sweep now **12/12 suites green**: sheet-mount 26, actions 24, resources 15, trackers 32, **slots 37**, attacks 47, noncaster 10, gear-bio 18, **rail 46**, orbs 14, forge 18, dice-engine 47. All touched JS `node --check` clean.

**Files this session (all staged in `/mnt/user-data/outputs/`, M deploys — deploy the four code files together, the class rename spans them):**
- **`sheet-mount.js`** — root `<main class="tok-sheet">` (dropped the bare `.sheet`).
- **`sheet-mount.css`** — base `color` / `font-family` / `line-height` on the `.tok-sheet` root; header comment corrected to `.tok-sheet` and documents the self-containment principle.
- **`combat-sheet-float.css`** — (1) float-skin root rules `.sheet-float .sheet` → `.sheet-float .tok-sheet` (×2); (2) restored the per-tab appearance wallpaper — `.sf-page` made the pinned stage (`overflow:hidden`), new `.sf-bg` (full-bleed, behind) + `.sf-scroll` (the scroller, over it) + the seven `.sf-bg .tok-*` layer rules mirroring `appearance.js`'s `#tok-*` stack.
- **`appearance-float.js`** — `--ac-hue` setter retargeted to `.tok-sheet`.
- **`smoke-sheet-mount.mjs`** — two assertions retargeted to `.tok-sheet` (26/26). (Test infra — for the repo, not a runtime dep.)
- **`smoke-rail.mjs`** — drift fix: `future`-tab count `2` → `1` (Settings is a live tab). 46/46. (Test infra.)
- **`smoke-sheet-slots.mjs`** — drift fix: concentration assertions retargeted from the removed `[data-conc-banner]` to the shipped `.conc-strip` `[data-conc-val]` / `[data-conc-drop]`. 37/37. (Test infra.)
- (`seal-demo.html` — a standalone before/after demo of the visual outcome, built during the earlier "seal" framing; reference only, not for deploy. The shipped fix is the class-drop above, which reaches the same result by removing the collision at the source rather than neutralizing it.)

**STILL PENDING (carried, unchanged):** per-spell spellcasting-ability EDITOR (mocks approved; per-spell `ability` already rides each spell object); Soul Shards "Gap 2" feat-granted spells; Notes-tab Quill editor; attack EDITOR (`mock-attacks.html` approved); leveled-spell damage rolling; HUD→engine dice dedupe; the live-combat caveats (combatants-realtime re-bloom for conditions/initiative, the four PCs' wrong `structural` aggregate to rebuild from Roll20, the inert ribbon "+"). (The two pre-existing stale-smoke failures noted in the previous draft were diagnosed and fixed this session — see the validation note above.)

## This session (June 25, cont.) — repo audit + housekeeping pass

After the sheet-isolation + wallpaper fixes landed, ran a full **staged-vs-committed reconciliation audit** (report: **`REPO-AUDIT.md`**, staged) and worked down the loose-end list. Method: `node --check` every JS/MJS, run all 22 smokes, verify every HTML `<script>`/`<link>` + JS `import`/`loadScript` resolves, cross-check every filename CONTEXT.md names against the repo, scan for strays.

**Repo is fundamentally healthy.** All **56 JS/MJS parse clean**; every asset reference resolves; **all 6 Netlify functions present** under `netlify/functions/` (the audit's first pass false-flagged `items2`/`members`/`member-invite`/`characters-export[-nightly]`/`character` by checking root only — they're all there); and **every feature behind a "missing" smoke is present and working** (appearance-settings, the rail Settings tab, combat-orbs). The drift is narrow: a few stale/broken/uncommitted smokes, ~13 uncommitted DB migrations, one stray file.

**DONE / staged this pass:**
- **`smoke-appearance-settings.mjs` — WRITTEN (18/18), staged.** Restores the coverage the June-24 file list claimed but never committed. It's an ES module (imports the real `appearance-settings.js` after seeding jsdom globals — not an eval'd `<script>` like the inline-script smokes). Proves: the customizer builds into `[data-rail-pane="settings"]` (`.tr-appearance.tok-appearance` host, populated by `buildAppearancePanel`); `appearance.css` is injected once on demand; `mount()` is idempotent and no-ops when the pane is absent; the live preview is routed onto **every** open `.sf-page` (via `applyFloatAppearance`'s own layer-injection + `.tok-grain` texture stamp — a jsdom-safe standard prop, not the flaky `--bg-*` custom props) and **not** the page (asserts no `#bg`/`#fx` page bleed); `paintFloats()` paints a freshly-opened float and no-ops with zero floats.
- **`migrations/README.md` — scaffolded, staged.** New version-controlled home for DB migrations + the convention (one `.sql` per change, sorts in apply-order, append-only, capture RLS/SECURITY-DEFINER too). Lists the ~13 deltas applied live but never committed (`advance_turn`, `characters_party_edit`, `enable_characters_realtime`, `grant_service_role`, `migrate_characters_data`, `schema_delta_aura`/`campaign_config`/`characters`/`characters_1b`/`characters_fk`/`saved_monsters`/`vitals_hp_sync`, `update_liadan_portrait`) for M to paste from the Supabase dashboard history. The ~10 already-committed deltas are left at repo root for now (move in when convenient). **(This "~13 named files" framing is SUPERSEDED — see "Migrations / DB reproducibility" at the very end of this entry, reconciled against the actual live schema.)**

**RECONCILED (claims vs reality):**
- **`smoke-rail-settings-tab.mjs` is NOT needed — claim trimmed.** "Settings tab is live" is covered by the **`smoke-rail.mjs`** 2→1 future-tab fix, and the Settings **pane** is now covered by the new `smoke-appearance-settings.mjs`. (Supersedes the "restore it from the June-24 session if wanted" note in the June-25 validation block above — between rail + appearance smokes the feature is covered; a third overlapping test adds nothing.)

**FOR M — quick deletions (C can't stage a deletion; run these):**
- `git rm "shards .html"` — a **stray older copy** of `shards.html` (2,551 vs 2,575 lines, **referenced by nothing**). Risk: editing the wrong file someday.
- `git rm smoke-sheet-data.mjs` — **broken + tests dead code.** It reads a hardcoded absolute path `/home/claude/out/mockup-sheet-visual-direction-v11.html` (a mockup never committed → only runs on M's machine), and it exercises **`sheet-data.js`, the decoupled twin nothing imports** (`soul-shards-spellcasting.js` only name-drops it in a comment). The real sheet render is covered by `smoke-sheet-mount` (26) + the sibling sheet smokes. **`sheet-data.js` itself is dead-code** — `git rm sheet-data.js` too if you want, M's call.

**RESOLVED — `smoke-world-marks.mjs` retired (M's call, this turn).** The smoke tests a **rail-integrated marks pane** (`buildMarksPane` / `registerMarksTab` / `injectMarksRailCss` / `renderMarkDetail` / `saveMarkNote` — reparenting `#marks-filter-row`/`#marks-list`/`#marks-place-btn` into a `.tr-panes` rail pane, master-detail, scoped CSS injected once). That approach was **tried and deliberately abandoned — "it didn't work very well"** — so the committed `world.html` with the standalone `#marks-panel` `<aside>` is the **intended state, NOT a regression.** Verified the three rail-marks identifiers appear **nowhere but the smoke itself** (no orphaned dead code in `world.html` or anywhere else), so the smoke is the lone remnant → `git rm smoke-world-marks.mjs`. **Next session: do NOT re-flag the standalone marks aside as a missing feature — it's the chosen design.**

**P4 (stale names):** `forge.html` / `soul-shards.html` mentions in the June-18 entries are **period-accurate history** ("M renamed it from `forge.html`") — left as-is to keep provenance. `sheet-prototype.html` key-facts note corrected (already absent from the repo).

**Three files retired this pass** — run together:
```
git rm "shards .html"        # stray older copy of shards.html, referenced by nothing
git rm smoke-sheet-data.mjs  # broken hardcoded path + tests the dead sheet-data.js twin
git rm smoke-world-marks.mjs # lone remnant of the abandoned rail-marks approach
```
(`sheet-data.js` itself is dead — `git rm sheet-data.js` too if wanted, M's call.)

**Suite status:** green suites gain `smoke-appearance-settings` (18); the two former reds are now *retired*, not failing. With the three deletions done and the staged FIX-3 smokes (`smoke-rail` 46, `smoke-sheet-slots` 37) + `smoke-appearance-settings` (18) deployed, the suite is **honestly green, no stragglers.**

### Migrations / DB reproducibility — reconciled against the LIVE schema (supersedes the "~13 files" list above)

Chased the migrations gap to ground and the file-list approach turned out to be unreliable — corrected via M's **live-schema dump** (Supabase's "context only" table export).

**The "59 missing migrations" was a false alarm.** Supabase's SQL editor showed **59 entries** — but those are saved *snippets* (every query M ever ran: re-runs, `SELECT`s, experiments, fixes-on-fixes), **not** 59 migrations. Deleting/closing a snippet does nothing to the DB (the schema lives in the DB regardless). So the real distinct-migration count is far smaller, and curating the 59 by hand is the wrong job.

**Ground-truth reconciliation (live DB = 10 tables):** `profiles` · `encounters` · `combatants` · `drawings` · `feed` · `campaign` · `scenes` · `characters` · `saved_monsters` · `roster_layout`. The committed SQL **creates 7** of them. **Three are created NOWHERE in the repo:**
- `public.characters` — the full table DDL (committed `key_open.sql` only *patches* it).
- `public.saved_monsters` — the Bestiary shelf.
- **`public.drawings`** — the AoE / fog layer. **It appears in NO session log**, so both the audit and the README's first draft missed it — only the live-schema dump surfaced it. (This is the proof that the named-file approach can't be trusted.)

Plus **column drift** (`combatants.aura`, `campaign.config`, and a few combatants columns added by later ALTERs), and — crucially — the entire **behavioral layer is uncommitted AND not shown by the table dump**: RLS policies, functions (`advance_turn`, the vitals↔hp sync fn, `combatants_guard_columns`, …), triggers, `service_role` grants, realtime-publication membership.

**`migrations/README.md` was REWRITTEN to this ground truth** (the authoritative reconciliation now lives there — table-by-table coverage, the 3 uncommitted tables, the behavioral-layer caveat, the capture method). The earlier "paste these ~13 named files" framing is retired.

**Capture method (decided): a single schema dump, NOT snippet-curation.** One `pg_dump --schema-only` (or `supabase db dump --schema public -f schema.sql`) from a real computer captures tables + policies + functions + triggers + grants in one runnable file. Use the **Session pooler** connection string (Dashboard → Connect → Session pooler) on port **5432** — the direct `db.<ref>.supabase.co` host is **IPv6-only** on the free tier and hangs on IPv4 home networks; username is `postgres.<ref>` (not bare `postgres`). After restore, realtime-publication membership must be re-enabled via Dashboard → Database → Publications (not in a schema dump).

**STATUS: PARKED (non-urgent).** Supabase auto-backs-up the live DB, so this is a reproducibility nicety, not disaster insurance — nothing blocks play or deploy. Tooling fought us this session and isn't worth more time right now: `npx` route needs Node (not installed on M's Mac); **pgAdmin 4 fatally errors on macOS without Homebrew** (its bundled Python `cryptography` hardcodes the brew `/usr/local/opt/openssl@3/` path → `dlopen libssl.3.dylib not found` on startup). Clean no-Homebrew fallback when M picks this up: **Postgres.app** (postgresapp.com) → gives a real `pg_dump` binary → one terminal command with the Session-pooler URL. Do it whenever the tooling cooperates.
