# CONTEXT Forge update · 2026-07-16c

Supersedes `CONTEXT_Forge-update-2026-07-16b.md` as the current concise Forge authority.

## Field verdict

The initiative-authority correction is field-confirmed. Líadan now receives the correct `DEX +1` and `Jack of All Trades +1` without an invented negative reconciliation term.

The canonical `/forge/` promotion, Workshop/Table identity, Planned tab, prototype retirement, player-side enemy-AC redaction, reaction hardening, and continuous player-vision foundation remain current.

## Phase 2f.4 — structural bridge completion

The active bridge sequence is implemented through the trustworthy structural contract.

### Stable identity

- Generated bridge IDs are derived from the exact ordered path and elevation signature rather than list position.
- Save/load, refresh, and both clients retain the same bridge ID and path identity.
- Live `connector_state` edits carry `path_signature` proof.
- A proof mismatch refuses a stale edit rather than applying it to another bridge.

### Replay-safe state

- Open, Closed, and Broken remain authoritative replay facts.
- Replay also stores the bridge path proof.
- Rewind/correction that removes an override restores the snapshot baseline.
- Live-state application does not mutate the immutable map snapshot or fingerprint.

### Movement and occupancy

- Open bridges support every authored path segment.
- Closed and broken bridges remove only the interior span.
- Land endpoints remain ordinary usable terrain.
- Occupancy prevents closing/breaking only when a creature stands on the interior span; a creature on the bridgehead does not falsely block the edit.

### Rail cover

- Rails are canonical geometry, not decoration.
- Target-side bridge rails establish at least half cover when the body-sample threshold is met.
- Cover evidence identifies `bridge-rail` as the blocker.
- Closed and broken bridges remove rail cover with the physical span.

### Staff operation

The active Height / Vertical Geometry panel now provides:

- stable bridge identity and span information;
- current state and occupancy status;
- movement and rail-cover audit indicators;
- Inspect, Open, Closed, and Broken controls;
- an Audit Bridges command;
- endpoint and path markers for the selected bridge.

Structural bridges remain active Phase 2 work and are not moved into Planned.

## Browser gate

Before beginning doors, run one focused two-browser bridge pass:

1. generate a map with at least one bridge;
2. confirm both devices show the same stable ID and path;
3. save/refresh/reconnect and confirm identity remains unchanged;
4. cross the open span in both directions;
5. verify rail cover from both sides;
6. close and break the bridge and confirm interior traversal disappears while land endpoints remain usable;
7. confirm an interior occupant blocks the state change;
8. confirm an endpoint occupant does not;
9. rewind/correct the edit and confirm the snapshot baseline returns;
10. run Audit Bridges and confirm the card is green on both clients.

## Next active sequence

After the focused bridge field pass, begin **doors** as the next connector family. Then proceed to tunnels, fords/hazardous crossings, semantic spawns, and validation/repair in the recorded Phase 2 order.
